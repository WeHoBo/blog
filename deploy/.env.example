# 好啵博客 —— compose 用环境变量（复制为 .env 后填写真实值）
#   cp .env.example .env && vi .env && chmod 600 .env
# 注意：.env 已被 .gitignore 忽略，不要提交。

# MySQL root 密码（务必用强密码，例如 openssl rand -base64 24 生成）
MYSQL_ROOT_PASSWORD=

# Redis 密码（同上；设置后必须同步更新 application-prod.yml 的 spring.data.redis.password）
REDIS_PASSWORD=
