# P0 三项落地改动报告

> 对应文档：`开源项目参考与改进建议.md` 第 5 节的三个 P0 首选。
> 本次**只改了本地代码**，服务器操作在文末给出命令。**未改数据库、未改配置、未改 nginx。**

---

## 一、一句话结论

三项都做完了，另外顺手修掉 2 个在做功能时**踩出来**的真实 bug。
**新增路由由 Nuxt SSR 直接提供，所以部署时不需要动 nginx。**

| # | 功能 | 状态 |
|---|---|---|
| 1 | 正文图片点击放大（灯箱） | ✅ 完成，含多图切换/缩放/键盘/手势 |
| 2 | 代码块升级：文件名 + 行高亮 + 折叠 + 换行开关 | ✅ 完成 |
| 3 | 标签云 / 分类独立落地页 + sitemap | ✅ 完成 |
| 额外 | 小图标（徽章）混进多图轮播 | ✅ 修掉 |
| 额外 | 图片被链接包着时链接点不动 | ✅ 修掉 |

---

## 二、改了哪些文件

### 新增（6 个）

| 文件 | 作用 |
|---|---|
| `blog-web/utils/imageLightbox.ts` | 图片灯箱实现（零依赖，约 330 行） |
| `blog-web/utils/taxonomy.ts` | 标签/分类页公共工具：路由参数匹配、规范 URL、分类建树 |
| `blog-web/pages/tags/index.vue` | `/tags` 标签云（字号随文章数变化）+ 全量标签列表 |
| `blog-web/pages/tag/[slug].vue` | `/tag/{slug}` 标签文章列表（分页 + 独立 SEO） |
| `blog-web/pages/categories/index.vue` | `/categories` 分类总览（父子分组卡片） |
| `blog-web/pages/category/[slug].vue` | `/category/{slug}` 分类文章列表（分页 + 独立 SEO） |

### 修改（7 个）

| 文件 | 改动 |
|---|---|
| `blog-web/components/MarkdownRenderer.vue` | 解析围栏元信息；行高亮/折叠；按钮组；挂灯箱 |
| `blog-web/assets/css/main.css` | 代码块新样式 + 灯箱样式（约 +110 行） |
| `blog-web/components/Header.vue` | 导航新增「分类」「标签」（桌面 + 移动菜单） |
| `blog-web/pages/index.vue` | 热门标签链接改指 `/tag/{slug}`（2 处） |
| `blog-web/pages/article/[id].vue` | 文章底部标签链接改指 `/tag/{slug}` |
| `blog-web/pages/post/[slug].vue` | 补上标签入口（此前这个页面完全没有标签） |
| `blog-server/.../controller/SitemapController.java` | sitemap 补 `/tags`、`/categories` 及各标签/分类落地页 |

---

## 三、三项功能逐个说明

### 1. 正文图片点击放大（灯箱）

**读者效果**：正文里所有正常大小的图片，鼠标移上去变成放大镜光标，点一下全屏。

| 交互 | 操作 |
|---|---|
| 打开 | 点击图片 |
| 关闭 | `Esc` / 点图片外的黑色区域 / 右上角 ✕ |
| 多图切换 | 左右箭头按钮 / 键盘 `←` `→` / 手机上左右滑动 |
| 缩放 | 滚轮 / 双指捏合 / 点一下图片（1× ↔ 2×） / `+` `-` |
| 放大后移动 | 鼠标拖拽 / 单指拖动（自动限制在可视范围内） |
| 图注 | 取图片的 `alt`（没有则取 `title`） |
| 图片挂了 | 显示「图片加载失败」+ 斜纹占位，不出现破图 |

**为什么要手写而不引 medium-zoom / PhotoSwipe**：medium-zoom 没有多图切换和键盘导航；
PhotoSwipe 要多引一个 JS + 一个 CSS 还得覆盖它的暗色主题。本站诉求就是「看大图 + 左右翻」，
手写约 330 行能全部覆盖，且直接复用 `main.css` 的暗色变量。

**两种「不该劫持点击」的情况已处理**（这也是下面两个额外 bug 的来源）：
- 图片外面包着链接且链接指向**别的页面**（例如 `[![图](a.png)](https://项目主页)`）→ **不挂灯箱**，链接照常跳转。
- 图片外面包着链接且链接指向**图片本身**（`[![缩略图](thumb.png)](a-full.png)`）→ 挂灯箱，并且**放大的是 `href` 那张大图**而不是缩略图。
- 原始尺寸小于 48×48 的图（徽章、emoji、图标）→ 不挂灯箱。

### 2. 代码块升级

**作者视角 —— 现在可以这样写代码块**（`title` 和 `{}` 都是可选）：

````markdown
```java title="UserService.java" {3-6}
public User find(Long id) {
    // ...
}
```
````

渲染结果：代码块左上角出现 `UserService.java` 文件名标签，第 3–6 行带蓝色左侧色条 + 淡蓝底。

| 能力 | 说明 |
|---|---|
| 文件名 | `title="xxx"`，左侧标签显示文件名（等宽字体、不做大写转换）；不写 `title` 则退回显示语言名（如 `JAVA`） |
| 行高亮 | `{3-5,8}` 或 `{7}`，支持逗号分隔多段；数字反着写 `{5-3}` 会自动纠正 |
| 默认折叠 | **超过 30 行**的代码块默认折叠到 22rem，底部渐隐，右上角出现「展开」按钮 |
| 复制 | 右上角「复制」（原有能力，行为不变） |
| 换行开关 | 右上角「换行」——长行代码默认横向滚动，点一下变成自动折行，适合窄屏 |

**为什么用 30 行做阈值**：一屏大约能放下 30 行的代码，更长的块会明显把正文顶下去。

### 3. 标签云 / 分类独立落地页

| 新 URL | 页面 |
|---|---|
| `/tags` | 标签云：字号按文章数缩放（13→27px，用平方根压差距）+ 全量标签网格 |
| `/tag/{slug}` | 该标签的文章列表，带分页 |
| `/categories` | 分类总览：一级分类卡片，子分类作为卡片内的胶囊 |
| `/category/{slug}` | 该分类的文章列表，带分页（**后端 `categoryId` 查询会自动包含子分类**） |

**SEO 修复了什么**：

- 之前标签/分类都跳回 `/?tagId=3`，**没有独立 URL**；现在每个聚合页有自己的地址。
- 之前 `canonical` 统一指向 `/post/{slug}` 和首页，等于告诉搜索引擎「这些聚合页别收录」；
  现在每个聚合页声明**自己的 canonical**（`https://codeup.asia/tag/{slug}`），配合独立
  `title` / `description`。
- `sitemap.xml` 从「首页 + 文章」扩到「首页 + 文章 + `/tags` + `/categories` + 每个有文章的标签/分类」。
  **只收录确实有文章的标签/分类**，空聚合页不进 sitemap（避免低质页面）。
- 空标签/空分类（0 篇文章）在落地页里也被过滤掉，理由是同一件事。

**一个必须处理的细节**：`Tag` / `Category` 在库里 `slug` 可能为空，而新建时后端会把空 slug 兜底成**中文名**。
所以路由参数按 `slug → name → id` 依次匹配，保证老链接、中文 slug（URL 编码后）都能打开；
生成链接时优先用 slug、没有则退回 id。这个规则在前端 `utils/taxonomy.ts` 与后端
`SitemapController` 里**保持完全一致**，否则 sitemap 里的 URL 会和页面 canonical 对不上。

---

## 四、顺手修掉的两个真实 bug

这两个不是「新功能」，是做功能时发现的既有问题：

**Bug 1：小图标会混进多图轮播**
第一版实现里，20×20 的徽章虽然点上去没反应，但**仍被算进灯箱的图片列表**。
后果是读者点「下一张」会翻到一张小徽章，而且计数器显示的图片总数会偏多。
修法：图片加载完成、确认是小图标后，把它从灯箱集合里**彻底移除**（清掉 `zoomSrc` 标记），
而不是只收回光标。现在轮播里只会出现真正的正文配图。

**Bug 2：图片被链接包着时，链接点不动了**
原始实现无条件 `preventDefault()`，于是 `[![封面](a.png)](https://项目主页)` 这种写法里，
读者点封面图**跳不到项目主页**（而是打开灯箱）。这是个很隐蔽的问题——作者不会想到。
修法：按链接指向决定行为（见上面第 1 项的三种情况）。

---

## 五、验证证据（区分「我核实过的」与「没核实的」）

### 已核实（都是本机实际跑出来的）

| 验证 | 命令/方式 | 结果 |
|---|---|---|
| 后端编译 + 单元测试 | `java -classpath ... classworlds ... -B -o test -pl blog-front-api -am` | **BUILD SUCCESS，39 tests，0 failures** |
| 全部 `.vue` 语法 | `@vue/compiler-sfc` 全量编译 43 个文件 | **43 checked，0 problems** |
| 两个新 `.ts` | `esbuild` 转译 | 均通过 |
| 围栏元信息解析（关键前提） | node 里用真实 `markdown-it` + `highlight.js` 复现 `highlight(str, lang, attrs)` | **28 项断言全过**：确认 `title=` 与 `{3-5}` **确实**能通过第 3 个参数拿到；30 行不折叠 / 31 行折叠边界正确；11 种语言的行数都对齐（防止 hljs 输出行数错位导致标错行） |
| `taxonomy.ts` 行为 | esbuild 编译后 node 断言 | **59 项断言全过**（含 slug/name/id/中文/URL 编码/null 输入/建树） |
| class 名双侧一致 | 脚本交叉核对「源码出现的 class」vs「main.css 里有没有规则」 | 全过。这类检查专门用来抓「改了名但样式没跟上」的静默失效 |
| 灯箱交互 | **jsdom 真实 DOM 实跑** | **45 项断言全过**：绑定规则 4 种写法、幂等、计数/图注、左右切换与循环、`←`/`→`/`Esc`、滚轮缩放与 1×–5× 夹取、锁滚动与还原、点背景关闭、点图切缩放、重复打开不叠加 |

### 没核实的（需要你在真实环境确认）

- **完整 `npm run build` 没跑过**。本机沙箱会拦截 `nuxi build` 删除 `.nuxt`/`dist` 的批量删除操作。
  替代验证是「43 个 `.vue` 全量 SFC 编译 + 两个新 `.ts` 转译」，能抓语法/模板错误，
  但**抓不到打包期的类型错误或循环依赖**。这是本次唯一必须靠服务器构建兜底的部分。
- **真实浏览器上的双指缩放 / 滑动切换没实测**。jsdom 里是合成事件，手势必须人工在手机上点一下。
- **前后端联调没跑过**。落地页依赖 `/article/tags`、`/category/list`、`/article/list?tagId=`，
  我只核对了接口存在与返回结构，没实际发过请求。
- **标签/分类落地页在同源路由上是否与既有页面冲突**：理论上不冲突
  （`/tag` `/tags` `/category` `/categories` 都是新前缀），但没起服务实测。

---

## 六、部署步骤

**结论先说：不用改 nginx、不用改数据库、不用加配置项。**

理由：
- 4 个新 URL 由 Nuxt SSR 提供，nginx 的 `location /` 已经反代到 `127.0.0.1:3000`，新路由自动生效；
- `/sitemap.xml` 已经反代到后端，重启 `blog-api` 后就是新的 sitemap；
- 本次没有新增任何 SQL 变更、也没有新增配置键。

### 1. 本机提交并推送（需要你授权）

```bash
cd D:/cxdownload/Blog
git add blog-web/utils blog-web/pages/tags blog-web/pages/tag blog-web/pages/categories blog-web/pages/category
git add -u
git status          # 确认清单与上面第二节一致再提交
git commit -m "feat: 图片灯箱 + 代码块文件名/行高亮/折叠 + 标签分类独立落地页"
git push origin main
```

推送后用 `git ls-remote origin main` 复核远端是不是真的更新了（不要只看本地 `origin/main`）。

### 2. 服务器部署（deploy.sh 会在服务器上完成构建）

```bash
ssh <你的服务器>
cd <服务器上的项目目录>          # 即 deploy/ 的上一级
git pull
sudo bash deploy/deploy.sh
```

脚本会依次：后端 `mvn package` → 前端 `npm install && npm run build` → 部署 `.output`
到 `/opt/blog/web/.output` → 同步 systemd unit → 重启 `blog-api` 和 `blog-web`。

### 3. 部署后确认

```bash
# 后端起来了、sitemap 里有新增的聚合页
curl -s https://codeup.asia/sitemap.xml | grep -c "/tag/"
curl -s https://codeup.asia/sitemap.xml | grep -c "/category/"

# 新路由返回 200 而不是 404
for p in /tags /categories; do echo -n "$p -> "; curl -s -o /dev/null -w "%{http_code}\n" https://codeup.asia$p; done

# 服务状态
systemctl --no-pager status blog-api blog-web | head -20
```

---

## 七、自测清单（建议逐条点一遍）

**代码块**
- [ ] 打开一篇带代码块的文章，代码块右上角出现「复制 / 换行」两个按钮
- [ ] 点「换行」→ 长行折行；再点一次还原
- [ ] 找一段超过 30 行的代码 → 默认折叠、右下有渐隐、「展开」按钮可切换
- [ ] 写一个 `title="a.js"` 的代码块 → 左上角显示 `a.js`（不是 `A.JS`）
- [ ] 写一个 `{3-5}` 的代码块 → 第 3–5 行有蓝色左条
- [ ] 点「复制」→ 粘到编辑器里，换行位置与原文一致、末尾不多空行

**图片灯箱**
- [ ] 点正文配图 → 全屏；`Esc` 关闭
- [ ] 多图文章：左右箭头 / 键盘 `←` `→` 能切换，计数器正确
- [ ] 滚轮 / 双指缩放能放大缩小，放大后能拖动
- [ ] 手机上左右滑动能切图
- [ ] 点一下图片 → 放大到 2×；再点一下 → 还原
- [ ] 文章里若有 `[![缩略图](小图)](大图)`，点开应该是**大图**
- [ ] 文章里若有 `[![图](a.png)](外链)`，点图应该**跳转外链**、不弹灯箱
- [ ] 文章里若引用了徽章（shields.io 那种小图），点它**不该**弹灯箱
- [ ] 关掉灯箱后页面能正常滚动（没有被锁住）

**标签/分类落地页**
- [ ] 顶部导航出现「分类」「标签」，点进去正常
- [ ] `/tags` 里字号大小随文章数变化；点任意标签进 `/tag/xxx`
- [ ] `/tag/xxx` 里文章列表正确、分页可用
- [ ] `/categories` 里父分类卡片下能看到子分类；点进去文章包含子分类的文章
- [ ] 首页右侧「热门标签」和文章底部的标签，点进去都是 `/tag/xxx`（URL 不再是 `/?tagId=`）
- [ ] 文章页（`/post/xxx`）正文下方能看到标签
- [ ] 浏览器直接访问一个不存在的标签（如 `/tag/nonexistent`）→ 显示 404 页而不是白屏
- [ ] 查看网页源码，`/tag/xxx` 的 `<link rel="canonical">` 指向 `https://codeup.asia/tag/xxx`（不是首页）

---

## 八、已知限制（如实列出，都不是这次引入的）

1. **`/post/{slug}` 与 `/article/{id}` 仍是两个页面，且 canonical 指向的是内容更少的那个。**
   `/post/{slug}` 只有标题 + 正文 + 新增的标签；而 `/article/{id}` 有目录、评论区、相关推荐、
   作者卡。也就是说**搜索引擎收录的规范页，反而是功能更少的那个**。这是之前 UI 报告里的
   P0 遗留项（「文章详情页合并」），本次只补了标签入口，没有合并页面——合并会动到评论区/目录
   等一大片逻辑，值得单独一批来做。

2. 客户端路由跳转到不存在的标签时不会展示 404（会显示空列表）。
   服务端直出的请求会正确返回 404，所以对 SEO 无影响，只是从站内跳过去时体验略糙。

3. 首页的分类筛选仍然是页内状态（点分类不改变 URL），没有改成 `/category/{slug}`。
   这是为了让首页的浏览体验保持原样；分类落地页是从顶部导航和 `/categories` 进入的。
   如果你希望**首页点分类也变成独立 URL**，那需要把首页的分类状态改成由路由驱动，是一次独立改动。

4. 代码块的「折叠 / 换行」状态不跨重渲染保留。在编辑器实时预览里改动正文会重置这两个开关
   （因为它们挂在 DOM 上，而 `v-html` 会整段重建）。读者侧不受影响。

5. 标签云只显示「有文章」的标签。如果你的库里有 0 篇文章的标签，它们不会出现在 `/tags`。

---

## 九、建议的下一批

按「性价比」排：

1. **文章详情页合并**（`/post/{slug}` 补上目录/评论/相关推荐，或让 `/article/{id}` 直接 301 到它）——
   改动面比这次三项加起来都大，但它是站内最大的一处结构性问题。
2. **RSS 订阅**（原建议第 4 项，P0）——照 `SitemapController` 再写一个 Controller 就行，成本很低。
3. **媒体库**（原建议第 5 项，P0）——`MediaLibraryDialog.vue` 已经存在，可以看看能不能复用。
