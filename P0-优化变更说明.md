# P0 优化变更说明（本地 → 服务器同步用）

- **代码优化提交**：`0c96942` — `perf: 列表查询收敛返回列，修复代码高亮主题丢失与生产环境错误栈泄露`
- **父提交**：`40ffa5c`（你上一次的安全修复提交）
- **后续提交**：`b6dd4b8`（本文档）、`4d4130c`（CI 配置修复，见第六节）
- **分支**：`feature/optimize-p0` → 已快进合并回 `main`，临时分支已删除
- **代码改动规模**：4 个文件，`+122 / −5`
- **影响范围**：不改数据库、不改任何 `application.yml` / 环境变量配置、不改接口路径与请求参数

---

## 一、部署须知（先看这个）

| 项目 | 是否需要动作 | 说明 |
| --- | --- | --- |
| 数据库 | ❌ 不需要 | 本轮零 SQL 变更，不需要执行任何 DDL/DML |
| 配置文件 / 环境变量 | ❌ 不需要 | 未改动 `application.yml`，未新增变量 |
| 后端 | ✅ 需要重新打包 | 替换 `blog-server` 的 jar 包 |
| 前端 | ✅ 需要重新构建 | 替换 Nuxt 构建产物（`.output/` 或静态文件） |
| CI 配置 | ❌ 不需要 | `4d4130c` 只改 `.github/workflows/ci.yml`，不影响服务器（见第六节） |

**要同步到服务器的文件（共 4 个）：**

```
blog-server/blog-front-api/src/main/java/com/blog/front/service/ArticleService.java
blog-server/blog-front-api/src/test/java/com/blog/front/service/ArticleServiceTest.java
blog-web/assets/css/main.css
blog-web/error.vue
```

其中 `ArticleServiceTest.java` 是单元测试，**不影响运行时**，同步只是为了保持代码库一致（CI 会跑它）。

**如果服务器是 `git pull` 部署**，直接 `git pull` 后在服务器上重新构建即可，无需手工改文件。

---

## 二、逐文件变更明细

### 1. `ArticleService.java`（3 处改动）

路径：`blog-server/blog-front-api/src/main/java/com/blog/front/service/ArticleService.java`

#### 1.1 `page()` — 前台文章列表加列白名单（第 74–79 行）

```diff
@@ -71,6 +71,12 @@ public class ArticleService {
             wrapper.orderByDesc(Article::getIsTop)
                     .orderByDesc(Article::getCreateTime);
         }
+        // 列表只需要展示字段，显式排除 content_md / content_html 两个 LONGTEXT，
+        // 否则每次翻页都会把整页文章的正文全文从数据库读回内存
+        wrapper.select(Article::getId, Article::getTitle, Article::getSummary, Article::getCover,
+                Article::getViewCount, Article::getCommentCount, Article::getIsTop,
+                Article::getCategoryId, Article::getCreateTime, Article::getWordCount,
+                Article::getUserId);
         return articleMapper.selectPage(new Page<>(pageNum, pageSize), wrapper);
     }
```

**修改原因**：原来没有 `.select()`，MyBatis-Plus 等于执行 `SELECT *`，会把 `content_md`、`content_html` 两个 LONGTEXT（文章正文）连同元信息一起从数据库读回内存。首页/列表页每翻一页，就要把该页 10–15 篇文章的**完整正文**读进 JVM 堆，然后大部分被丢弃。数据量越大、正文越长，内存和数据库 IO 浪费越明显。

**为什么可以安全排除这些列**：接口 `GET /api/article/list` 在 `ArticleController.list()` 里是**自己手工拼 Map 输出**的，只读取下面这些字段，白名单**刚好覆盖**：

`id`、`title`、`summary`、`cover`、`viewCount`、`commentCount`、`isTop`、`categoryId`、`createTime`、`wordCount`、`userId`

> 补充说明：前端 `ArticleCard.vue` 里有 `article.slug ? '/post/'+slug : '/article/'+id` 的写法，但 `/api/article/list` **从来不返回 `slug`**（控制器拼 Map 时就没放），所以那个 `slug` 分支从来没有生效过，链接一直走 `/article/{id}`。排除 `slug` 不会产生任何行为变化。

#### 1.2 `adminPage()` — 后台文章列表加列白名单（第 97–103 行）

```diff
@@ -88,6 +94,13 @@ public class ArticleService {
         }
         wrapper.orderByDesc(Article::getIsTop)
                 .orderByDesc(Article::getCreateTime);
+        // 后台列表同样只取元信息：排除 content_md / content_html / password 三个大字段
+        wrapper.select(Article::getId, Article::getTitle, Article::getSlug, Article::getSummary,
+                Article::getCover, Article::getStatus, Article::getUserId, Article::getCategoryId,
+                Article::getSeries, Article::getIsTop, Article::getViewCount,
+                Article::getCommentCount, Article::getLikeCount, Article::getWordCount,
+                Article::getIsDeleted, Article::getIsEncrypted, Article::getVisibility,
+                Article::getCreateTime, Article::getUpdateTime);
         return articleMapper.selectPage(new Page<>(pageNum, pageSize), wrapper);
     }
```

**修改原因**：同上，`GET /api/article/admin/list` 直接返回 `Page<Article>` 实体，原先 `SELECT *` 会把整个后台文章列表的**全部正文 + 密码字段**读出来并序列化回前端。

后台列表页 `pages/admin/articles/index.vue` 实际只用到 `id`、`title`、`isTop`、`status`、`viewCount`、`createTime` 六个字段（其余保留是为了小字段不必要地削减，且便于后续扩展）。这里额外排除了 `password`（文章加密密码），避免敏感字段进入后台列表接口响应。

#### 1.3 `related()` — 相关推荐不再返回正文（第 269–271 行）

```diff
@@ -253,7 +266,9 @@ public class ArticleService {
         if (article == null || article.getCategoryId() == null) {
             return List.of();
         }
+        // 相关推荐只渲染标题/封面/时间，不返回正文，避免响应体里塞进 4 篇完整文章
         return articleMapper.selectList(new LambdaQueryWrapper<Article>()
+                .select(Article::getId, Article::getTitle, Article::getCover, Article::getCreateTime)
                 .eq(Article::getCategoryId, article.getCategoryId())
                 .eq(Article::getStatus, 1)
                 .eq(Article::getVisibility, 0)
```

**修改原因**：这是本轮**影响最明显**的一处。`GET /api/article/{id}/related` 原本直接 `return Result.ok(List<Article>)`，返回的是**完整实体**——也就是说，用户每打开一篇文章，这个"相关推荐"接口的响应体里就塞进了 **4 篇文章的完整 HTML 正文**。

前端 `pages/article/[id].vue` 的"相关推荐"区块只渲染 `r.id`、`r.title`、`r.createTime`（链接固定为 `/article/${r.id}`），所以正文和摘要都是纯粹的浪费流量。手机端打开文章页时，这个浪费尤其明显。

---

### 2. `main.css`（2 处改动）

路径：`blog-web/assets/css/main.css`

#### 2.1 把 `@import` 移到文件最顶部（第 1 行）

```diff
@@ -1,9 +1,9 @@
-﻿@tailwind base;
+﻿@import 'highlight.js/styles/github.css';
+
+@tailwind base;
 @tailwind components;
 @tailwind utilities;
 
-@import 'highlight.js/styles/github-dark.css';
-
 html {
```

#### 2.2 新增暗色模式高亮配色块（第 81–124 行）

```diff
@@ -77,6 +77,51 @@ strong, b { color: #1f2937; }
   background: #1a1d23 !important;
   border-color: #2d3139;
 }
+
+/* 暗色模式语法高亮：等价于 highlight.js 的 github-dark 主题。
+   这里手写而不再 @import 一份，是因为两套主题若同时全局引入会互相覆盖，
+   无法按 .dark 切换到正确的那一套。 */
+.dark .hljs { color: #c9d1d9; background: #0d1117; }
+.dark .hljs-doctag,
+.dark .hljs-keyword,
+/* …… 中间为 github-dark 各 token 配色，共 41 条选择器 …… */
+.dark .hljs-addition { color: #aff5b4; background-color: #033a16; }
+.dark .hljs-deletion { color: #ffdcd7; background-color: #67060c; }
 .code-wrapper pre.hljs code { background: transparent !important; }
```

（完整内容见文件第 81–124 行；`github-dark` 的 token 配色共 20 组，覆盖 keyword / title / attr / string / built_in / comment / name / section / bullet / emphasis / addition / deletion 等）

**修改原因（这里要更正一个早前判断）**：

代码高亮本来完全不工作。真实原因**不是**"亮色模式对比度低"，而是 **`@import` 被构建管线整个丢弃了**——产物 CSS 里连一个 `hljs-*` 类都不存在。

根因是 CSS 规范：`@import` 必须写在文件所有规则之前（`@charset` 除外）。原来写成：

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import 'highlight.js/styles/github-dark.css';   /* ← 位置非法，被直接忽略 */
```

Tailwind / Vite 处理时直接丢弃这条 `@import`，所以主题从来没被加载过。

修法：
1. 把亮色主题 `@import 'highlight.js/styles/github.css'` 提到**文件第一行**，让它真正被内联；
2. 暗色主题不再引第二份 `@import`——因为两份全局主题会互相覆盖，无法通过 `.dark` 切换（后加载的那份永远赢）。改为手写 `.dark .hljs*` 规则块，靠 CSS 优先级实现浅色/深色自动切换。

**兼容性确认**：已有的 `.dark .code-wrapper pre.hljs { background: #1a1d23 !important; }` 特异性更高且带 `!important`，仍会生效，所以代码块的背景色行为没有变化；新增的 `.dark .hljs` 只兜住未被 `.code-wrapper` 包裹的代码块和 token 文字颜色。

---

### 3. `error.vue`（1 处改动，2 行）

路径：`blog-web/error.vue`

```diff
@@ -12,8 +12,8 @@
-      <!-- 调试信息：真实错误（帮助定位） -->
-      <div v-if="error?.message && error?.statusCode !== 404" class="text-xs ...">
+      <!-- 调试信息：仅在开发环境展示，生产环境不输出 message / 堆栈，避免泄露内部实现 -->
+      <div v-if="isDev && error?.message && error?.statusCode !== 404" class="text-xs ...">
         <p>{{ error.message }}</p>
         <p v-if="(error as any)?.cause?.stack" class="mt-1 opacity-70 whitespace-pre-wrap">{{ (error as any).cause.stack }}</p>
       </div>

@@ -34,6 +34,9 @@ import type { NuxtError } from '#app'
 const props = defineProps<{ error: NuxtError | null }>()
 
+// 仅开发环境展示真实错误信息；生产环境构建时该常量会被替换为 false，调试块整体被摇掉
+const isDev = import.meta.dev
+
 function handleError() {
```

**修改原因**：原代码在生产环境会把 `error.message` 和 `error.cause.stack`（完整调用堆栈）直接渲染到页面上。任何触发异常的用户都能看到你的类名、包名、文件路径甚至部分代码结构，属于信息泄露。加 `import.meta.dev` 守卫后，生产构建时该常量被静态替换为 `false`，整个调试块会被 tree-shaking 摇掉——**开发时你照样能看到堆栈，线上不再暴露**。

---

### 4. `ArticleServiceTest.java`（测试基建 + 2 条新用例）

路径：`blog-server/blog-front-api/src/test/java/com/blog/front/service/ArticleServiceTest.java`

#### 4.1 新增 `@BeforeAll` 初始化 MyBatis-Plus TableInfo

```java
+    /**
+     * LambdaQueryWrapper.select(方法引用...) 会立即解析列名，依赖 MyBatis-Plus 的
+     * TableInfo 缓存。这里全部 mapper 都是 mock，没有 Spring/MyBatis 启动过程，
+     * 缓存为空会抛 "can not find lambda cache for this entity"，故手动初始化。
+     */
+    @BeforeAll
+    static void initTableInfo() {
+        TableInfoHelper.initTableInfo(new MapperBuilderAssistant(new MybatisConfiguration(), ""), Article.class);
+    }
```

**修改原因**：这是本轮唯一"因为改动而必须配套修"的地方。`LambdaQueryWrapper.select(Article::getXxx)` 与 `eq()` / `orderByDesc()` 不同——**前者会立即解析方法引用对应的列名**，需要 MyBatis-Plus 的 TableInfo 缓存；而 `eq()` / `orderByDesc()` 是延迟到 SQL 生成时才解析的。

单元测试里所有 mapper 都是 Mockito mock，没有 Spring / MyBatis 启动过程，缓存为空 → 加了 `.select()` 后原有的 4 个 `page_*` 用例全部报 `can not find lambda cache for this entity`。手动初始化 TableInfo 后恢复。**运行时的 Spring Boot 环境不存在这个问题**（启动时自动初始化）。

#### 4.2 新增 2 条回归测试，直接断言生成的 SQL 列名

```java
+    @Test
+    void page_selectsOnlyListColumns_excludesArticleBody() {
+        ...
+        s.page(1, 10, null, null, null, null, "time");
+        String select = captor.getValue().getSqlSelect();
+        assertNotNull(select, "列表查询应显式指定返回列");
+        assertTrue(select.contains("title"));
+        assertTrue(select.contains("word_count"));
+        assertFalse(select.contains("content_md"), "列表不应读取正文 Markdown");
+        assertFalse(select.contains("content_html"), "列表不应读取正文 HTML");
+    }
+
+    @Test
+    void adminPage_selectsOnlyListColumns_excludesBodyAndPassword() {
+        ...
+        assertFalse(select.contains("content_md"));
+        assertFalse(select.contains("content_html"));
+        assertFalse(select.contains("password"), "后台列表不应下发密码字段");
+    }
```

**修改原因**：防止以后有人不小心把 `.select()` 删掉、导致正文大字段重新被全量读取。测试用 `ArgumentCaptor` 抓住传给 mapper 的 wrapper，再用 `Wrapper.getSqlSelect()` 读取真实列名做断言——**验的是实际生成的 SQL，不是注释**。

---

## 三、验证结果

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 后端编译 | ✅ 通过 | `BUILD SUCCESS` |
| 后端单元测试 | ✅ **23 个全绿**（原 21 + 新增 2） | `Tests run: 23, Failures: 0, Errors: 0, Skipped: 0` |
| CSS 主题内联 | ✅ 通过 | Tailwind 产物中 `.dark .hljs` 出现 41 次、`24292e` 4 次、`d73a49` 1 次、`0d1117`/`c9d1d9` 均存在 |
| 前端完整构建 | ✅ 通过 | GitHub Actions `CI #6`（run 34573260612）的 Frontend 任务执行了完整的 `npm test` + `npm run build`，整体 **Success**（1m12s） |

### 上线后建议自测的 4 个点

1. **列表查询列收敛**：打开首页并翻页，DevTools → Network 看 `/api/article/list` 响应体，应明显变小，且响应里不再有 `contentMd` / `contentHtml` 字段。
2. **相关推荐**：打开任意一篇文章，看 `/api/article/{id}/related` 响应，应只含 `id` / `title` / `cover` / `createTime`（过去这里会返回 4 篇完整正文）。
3. **代码高亮**：随便打开一篇带代码块的文章，浅色/深色切换都应正常着色（此前是完全无高亮）。
4. **错误页**：访问一个不存在的路径，确认 404 页正常；生产环境即使触发 500，也不应再显示错误堆栈。

---

## 四、本轮明确**没有**做的事

为了避免误会，以下是本轮**未改动**的内容:

- 数据库：零变更，未执行任何 SQL
- `application.yml` / 环境变量 / 部署脚本：零变更
- 接口路径、请求参数、响应结构：零变更（`related` 只是少返回了前端本来就没用到的字段）
- 以下 P1/P2 问题**仍未处理**（如需继续可另开一轮）：
  - `/post/{slug}` 与 `/article/{id}` 双路由并存，同一篇文章两个 URL（SEO 问题）
  - `layouts/default.vue` 中 `mousemove` 直接写 `style` 触发整页重渲染
  - `main.css` 中 `.code-lang-label` 重复定义两次
  - `ArticleService.getArticlesByTagId()` 是无人调用的死代码
  - 大量 emoji 当图标，而项目已装 `@nuxt/icon` 未用
  - 编辑器无草稿自动保存

---

## 五、回滚方式

```bash
# 撤销本次提交（保留改动在工作区）
git revert 0c96942

# 或直接把 main 退回上一个提交
git reset --hard 40ffa5c
```

服务器上若已重新构建，回滚后需要**重新构建并重新上传**后端 jar 与前端产物。

---

## 六、附带修复：CI 一直失败的问题（提交 `4d4130c`）

推送后才发现，仓库的 CI **从建立起一直是红的**（`CI #1`～`CI #5` 全部 Failure），与本次优化无关，
是仓库固有问题。既然要部署，顺手修掉了。

### 现象

`CI #4`（你自己的 `40ffa5c`）和 `CI #5` 失败点完全相同：前端任务 `Run unit tests` 步骤
`Process completed with exit code 1`，后端任务是通过的。

### 根因

```
Error: Transform failed with 1 error:
[TSCONFIG_ERROR] Failed to load tsconfig for 'composables/__tests__/useLayout.spec.ts': Tsconfig not found
Plugin: vite:oxc
```

`blog-web/tsconfig.json` 的全部内容只有一行：

```json
{ "extends": "./.nuxt/tsconfig.json" }
```

而 `.nuxt/` 在 `.gitignore` 里。CI 每次都从零 checkout，**没有 `.nuxt/` 目录**，
Vite 7 的 `vite:oxc` 转换器解析这个 `extends` 时找不到文件，直接抛错退出——
所以测试一个都没跑就失败了。本地之所以"看起来正常"，只是因为跑过 `nuxi dev` / `nuxi prepare`
生成过 `.nuxt/tsconfig.json`。

### 修复

`.github/workflows/ci.yml` 的 frontend 任务中，在 `Run unit tests` 之前插入：

```yaml
      - name: Prepare Nuxt
        run: npx nuxi prepare
```

这是 Nuxt 官方推荐的 CI 做法，让 Nuxt 先生成自己的 tsconfig 再跑测试。
**只改 CI 配置，不影响任何部署产物。**

### 结果

`CI #6`（run 34573260612）：**Success**

| 任务 | 结论 | 耗时 |
| --- | --- | --- |
| Backend (Maven test) | ✅ 通过 | 19s |
| Frontend (Vitest + Nuxt build) | ✅ 通过（含完整 `npm run build`） | 1m 12s |

仅剩 3 条非阻塞的弃用告警（`actions/checkout@v4`、`actions/setup-java@v4` 因 Node 20 弃用被强制跑在 Node 24 上；
建议后续升级到 `actions/checkout@v5`、`actions/setup-java@v5`），不影响构建结果。

> 对部署的影响：**无**。这一节只改了 `.github/workflows/ci.yml`，不需要同步到服务器。
