# 好啵博客 · 可优化项清单

> 生成时间：2026-09-11
> 基线：`main @ 5020218`
> 说明：本文档只列**待办**，本次会话已经修掉的项不在此列（见文末附录）。
> 标记说明：✅ = 我亲自读过代码核实过的；未标 ✅ 的来自自动化审计，建议动手前再确认一次。

---

## 🚨 第一批：安全与"会导致线上事故"的问题

### 1. ✅ 部署脚本与 systemd 的 jar 文件名不一致 → 部署静默不生效

- `deploy/deploy.sh:15` 把 jar 复制为 `$APP_DIR/blog-api.jar`
- `deploy/blog-api.service:10` 启动的却是 `/opt/blog/blog-front-api-1.0.0.jar`
- **影响**：脚本跑完提示成功、`systemctl restart` 也正常，但实际启动的是**上一次残留的旧 jar**。你会以为部署生效了，其实代码没更新。这是目前最危险的一条。
- **建议**：复制成完全一致的文件名；更稳的做法是复制到固定软链 `blog-api.jar` 并让 `ExecStart` 指向软链，部署后打印实际运行的 jar 校验和。

### 2. ✅ RAG 服务完全无鉴权，且监听 `0.0.0.0`

- `rag_system/backend/main.py:277` → `uvicorn.run("main:app", host="0.0.0.0", port=8000, reload=True)`
- `/upload`、`/chat`、`/free-chat`、`/documents`、`/history`、`/ingest-articles` **全部无任何鉴权**
- **影响**：只要 8000 端口对公网可达，任何人都能：
  - 无限调用 DeepSeek → **刷爆你的 API 账单**
  - 上传 PDF → 打满服务器磁盘
  - 读 `/documents`、`/history` → 泄露文档列表与全部聊天记录
  - `reload=True` 是开发模式，生产环境会额外消耗资源且不稳定
- **建议**：加 API-Key 依赖校验；`host` 改 `127.0.0.1`（只让 Java 侧通过内网调用）；`reload=False`；在 nginx 层再加一道。

### 3. ✅ 私密文章被导入了 RAG 知识库

- `rag_system/backend/import_blog.py:37` → `WHERE a.status = 1 AND a.is_deleted = 0`
- **影响**：**缺少 `visibility = 0` 过滤**。你标记为私密（`visibility=1`）的文章，其正文已经被切块灌进了向量库。配合上一条（RAG 无鉴权），任何人都能通过 `/chat` 提问检索到你私密文章的内容。
- **建议**：补 `AND a.visibility = 0`，然后清库重新导入一次（旧数据还在里面）。

### 4. ✅ sitemap 把你的私密文章交给搜索引擎

- `blog-server/.../controller/SitemapController.java:24-25` → 只过滤了 `status=1`、`is_deleted=0`
- **影响**：私密文章 URL 进入 `sitemap.xml`，会被搜索引擎抓取并可能收录。
- **建议**：补 `.eq(Article::getVisibility, 0)`。

### 5. ✅ CORS 允许任意来源 + 携带凭证

- `blog-server/.../config/CorsConfig.java:15-18` → `addAllowedOriginPattern("*")` 且 `setAllowCredentials(true)`
- **影响**：任意网站都能以你的用户身份发起带 Cookie/凭证的跨域请求并读取响应，等价于开放了 CSRF 通道。
- **建议**：只放行 `https://codeup.asia`（以及本地调试域名）；如果不用凭证就删掉 `setAllowCredentials(true)`。

### 6. ✅ Redis 反序列化开启了 autoType

- `blog-server/.../config/RedisConfig.java:46` 写入时 `WriteClassName`，`:54` 读取时 `JSONReader.Feature.SupportAutoType`
- **影响**：fastjson2 开 autoType 允许按 JSON 里的类名反序列化任意类，历史上是经典 RCE 利用点。只要 Redis 被写入恶意值（比如 Redis 未设密码暴露在内网/公网），就可能被利用。
- **建议**：关掉 `SupportAutoType`；或改用 `StringRedisTemplate` + 固定类型的 Jackson 反序列化。

### 7. ✅ nginx 缺少两处关键配置

- `deploy/nginx.conf:16-24` → 只有 `location /api/` 和 `location /`
- **影响**：
  - 没有 `client_max_body_size` → 默认 **1MB**，后台导入 Markdown/Word、上传图片超过 1MB 会被 nginx 直接 `413` 拒绝（Java 侧根本收不到请求）
  - `/api/` 没有 `proxy_read_timeout` / `proxy_buffering off` → 默认 60s 超时，而 AI 流式回答 Java 侧设的是 180s，**长回答必被 504 截断**
  - 没有 `/uploads/` 的 location → 上传的图片在线上 **404**（见第 55 条）
- **建议**：全局加 `client_max_body_size 20m;`；`location /api/ai` 单独加 `proxy_read_timeout 300s; proxy_buffering off;`。

### 8. 文件上传没有类型与大小白名单

- `blog-server/.../controller/UploadController.java:22` 直接接 `MultipartFile`，未见校验
- `blog-server/.../file/LocalFileStorage.java:23-41`、`OssFileStorage.java:47-61`
- **影响**：可以上传 `.html` / `.svg`，OSS 模式下返回公网 URL，浏览器按 `text/html`、`image/svg+xml` 渲染 → **存储型 XSS**。
- **建议**：只允许 `png/jpg/jpeg/gif/webp`，随机文件名、丢弃原始后缀，限制大小。

### 9. ✅ 后台文章详情把 `password` 字段返回给了前端

- `ArticleController.java:129` → `articleService.getAdminById(id)`；`ArticleService.java:132-138` 用的是 `selectById`（无列白名单）
- **影响**：`GET /api/article/{id}`（管理员身份）返回完整实体，包含文章加密密码的 BCrypt 密文。后台**列表**接口我们这次已经排除了 `password`，**详情**接口漏了。
- **建议**：详情接口也走列白名单或改成 VO 返回。

### 10. ✅ 评论自动通过审核，且无校验、无频率限制

- `blog-server/.../controller/CommentController.java:77` → `comment.setStatus(1)`
- **影响**：表里明明有 `status` 审核字段却直接置为"已通过"，任何人都能立刻发布任意内容；没有长度上限、没有频率限制，容易被刷垃圾。
- **建议**：默认 `status=0` 进审核队列（或加敏感词/频率限制）；补 `@Size` 长度校验。

### 11. ✅ OAuth 的 `state` 生成了但从不校验

- `blog-server/.../controller/OAuthController.java:53-54` 生成 `state` 并拼进授权 URL，回调处没有任何校验
- **影响**：OAuth CSRF / 登录绑定攻击（攻击者可诱导你把第三方账号绑到他的账号上，或反之）。
- **建议**：`state` 存进 Redis/会话并设有效期，回调时校验并一次性删除。

### 12. ✅ AI 回复经过 `html: true` 的 Markdown 渲染 → 存在注入链

- `blog-web/components/MarkdownRenderer.vue:27` → `new MarkdownIt({ html: true, ... })`；`:2` → `v-html="renderedHtml"`
- `blog-web/components/AiFloat.vue:71` → AI 回复用同一个组件渲染
- **影响**：AI 回答的内容来自 DeepSeek + RAG 检索到的**你自己上传的 PDF 和博客正文**。也就是说，一份构造过的 PDF 可以把 HTML 注入到大模型上下文里，模型照抄出来就变成 `v-html` 渲染的原生 HTML → **存储型 XSS**。
- **建议**：`html` 改 `false`；或引入 `DOMPurify` 净化后再 `v-html`；AI 回复建议走白名单渲染。

---

## 🎨 第二批：UI / 交互体验（本次重点）

### 13. ✅ 后台所有表格在手机上被裁掉且无法横向滚动

- `pages/admin/articles/index.vue:36`、`tags.vue:8`、`links.vue:8`、`users.vue:5`、`logs.vue:4`
- 外层都是 `rounded-lg shadow-sm overflow-hidden` 包着 `<table class="w-full">`
- **影响**：`overflow-hidden` 把超出宽度的列**直接裁掉且不可滚动**。「操作」「时间」等列在手机上看不见也点不到，**后台在移动端基本不可用**。
- **建议**：把 `<table>` 单独包一层 `<div class="overflow-x-auto">`，外层 `overflow-hidden` 只负责圆角。

### 14. ✅ 文章详情页在加载中/404 时，下方区块照样渲染

- `pages/article/[id].vue:12/15/19` 是 `v-if pending` / `v-else-if error` / `v-else` 三选一，
  但 `:65-150` 的「上一篇/下一篇」「作者卡」「评论区」「相关推荐」是它们的**平级兄弟节点**，不在 `v-else` 里
- **影响**：文章 404 时页面会同时显示"文章不存在"**和**一个空评论框＋返回链接，看起来像页面坏了。
- **建议**：把 63–150 整段移进 `v-else`（或加 `v-if="article"`）。

### 15. ✅ `dark:bg-gray-750` 这个色阶不存在，深色样式被静默忽略

- `pages/article/[id].vue:119`、`pages/admin/articles/create.vue:68`、`pages/admin/articles/[id].vue:65`、`pages/admin/articles/index.vue:51`
- **影响**：Tailwind 默认调色板里没有 `gray-750`，这几处在深色模式下没有预期背景（评论输入区、Markdown 预览区、列表 hover 行）。
- **建议**：换成存在的色阶，如 `dark:bg-gray-700/50`。

### 16. ✅ `.code-lang-label` 在 `main.css` 里重复定义，浅色样式被覆盖

- `assets/css/main.css:142-150`（浅色）与 `:232-238`（无作用域，靠后生效）
- **影响**：代码块右上角的语言标签，浅色模式下本该是蓝色半透明，实际被后面的深板岩灰覆盖，与设计意图不符。
- **建议**：删掉 232–238 那段，或合并成一套带 `.dark` 切换的定义。

### 17. ✅ 目录（TOC）三级缩进类被 Tailwind 清除 → h3 不缩进

- `components/TableOfContents.vue:5` → `:class="'pl-' + (item.level - 1) * 3"`
- **影响**：类名是**运行时字符串拼接**的，Tailwind 扫描源码时看不到 `pl-6` 这个字面量，于是不生成它。结果 h2 缩进正常、**h3 完全不缩进**，目录层级视觉错乱。
- **建议**：改成显式映射 `{2:'pl-3',3:'pl-6',4:'pl-9'}` 或加 `safelist`。

### 18. 18 处原生 `alert()` / `confirm()`

- `pages/admin/articles/index.vue:114,119,133,148,165,171,193,202`、`categories.vue:137`、`tags.vue:80`、`links.vue:107`、`articles/[id].vue:163,179`、`articles/create.vue:159,175`、`settings.vue:69`、`pages/profile.vue:69`
- **影响**：系统弹窗风格与站点完全脱节、无法样式化、会阻塞页面；`alert` 报错在移动端体验尤其差。
- **建议**：做一个统一的 `ConfirmDialog` + `Toast` 组件替换掉。

### 19. 页眉/页脚/设置弹窗硬编码粉色，不跟主题色

- `components/Header.vue:3,90`、`components/Footer.vue:2` → `bg-pink-50` 之类
- **影响**：全站卡片都是白底，只有这三处是粉色；主题色滑块改的是 `--primary`，改不动它们。换主题色后视觉不统一。
- **建议**：统一成 `bg-white/80 dark:bg-gray-900/80` 或走主题变量。

### 20. 几处装饰元素写死蓝色，不跟主题色走

- `components/StarryBackground.vue:4-23`（`#60a5fa`/`#818cf8`/`#8b5cf6` 等）
- `components/TableOfContents.vue:95,99,101,104`（`#3b82f6`/`#2563eb`）
- `pages/login.vue:93`（粒子色 `#60a5fa`）
- **影响**：把强调色调成绿/橙后，背景光斑、目录高亮、登录页粒子仍是蓝紫，整体氛围割裂。
- **建议**：改用 `hsl(var(--ph) ...)` 或已有的 `theme-text` / `var(--primary)`。

### 21. 后台保存按钮没有 loading / disabled，可重复提交

- `pages/admin/articles/create.vue:6-8`、`pages/admin/articles/[id].vue:6-8`
- **影响**：网络慢时连点会重复创建/重复保存文章。
- **建议**：加 `:disabled="saving"` 和「保存中…」文案。

### 22. 文章编辑器两栏布局写死，移动端不堆叠

- `pages/admin/articles/create.vue:66-71`、`pages/admin/articles/[id].vue:63-68` → `grid-cols-2` + 固定 500px 高
- **影响**：手机上编辑/预览并排被压成两条细缝，基本没法写文章。
- **建议**：`grid-cols-1 md:grid-cols-2`，移动端上下堆叠。

### 23. 后台顶部导航 7 个链接没有响应式处理

- `layouts/admin.vue:12-20` → `<nav class="flex gap-4 text-sm">`
- **影响**：手机上「文章/分类/标签/友链/用户/日志/设置」＋「退出」一行放不下，横向溢出。
- **建议**：窄屏折叠成抽屉或横向滚动。

### 24. 首页右侧「热门文章 / 热门标签」在窄屏被直接隐藏且无替代入口

- `pages/index.vue:149` → `hidden xl:block`
- **影响**：平板/手机用户完全看不到热门内容和标签云；而左侧分类是有抽屉替代的，两边不对称。
- **建议**：移动端补一个可折叠区块，或把标签云放到主内容流底部。

### 25. 全站没有键盘焦点样式

- `assets/css/main.css` 里全文没有 `:focus` / `focus-visible` / `outline` 规则
- **影响**：键盘 Tab 用户看不到当前焦点在哪，无障碍性不达标。
- **建议**：加全局 `:focus-visible { outline: 2px solid var(--primary); outline-offset: 2px }`。

### 26. 后台改用户密码的输入框是明文

- `pages/admin/users.vue:66` → `type="text"`（个人中心 `pages/profile.vue:34` 是正确的 `type="password"`）
- **影响**：后台改密码时明文显示，有旁窥泄露风险，且两处行为不一致。
- **建议**：统一为 `type="password"`。

### 27. 后台主按钮色阶不统一

- `pages/admin/tags.vue:5` 用 `bg-primary-500`，其余页面（`articles/index.vue:14`、`links.vue:5`、`users.vue:71`）用 `bg-primary-600`
- **影响**：「添加标签」按钮明显比其他主按钮浅。
- **建议**：统一到 `bg-primary-600`。

### 28. 若干无障碍细节

- `components/Pagination.vue:10-14` 分页数字按钮缺 `aria-label`
- `components/MarkdownRenderer.vue:102` mermaid 弹窗关闭按钮只有 `×`，无 `aria-label`
- `pages/about.vue:17` 友链头像 `<img>` 缺 `alt`
- `components/ArticleCard.vue:15`（网格用 `<h3>`）与 `:33`（列表用 `<h2>`）标题层级不一致
- **建议**：逐条补上即可，成本很低。

### 29. 一些小的冗余/不一致

- `pages/about.vue:6` 的 `prose dark:prose-invert` 是死类（`tailwind.config.ts:29` 的 `plugins: []` 没装 typography 插件），靠 `article-content` 兜底才没出问题
- `pages/index.vue:302,309` 的 `CAT_ICONS` 重复定义了 `'Docker'`
- `pages/login.vue:44` 标题写「登录账号」但页面只有第三方 OAuth 按钮，没有账号密码表单
- `assets/css/main.css:277` 的 `neon-glow` 只在 `.dark` 下定义，浅色模式是无效类

---

## ⚡ 第三批：性能 与 SEO

### 30. ✅ 首页 SSR 首屏只有一个空骨架屏（全站最大的 SEO 问题）

- `pages/index.vue:424` → `init()` 在 `<script setup>` 顶层**直接调用且没有 await**，也没用 `useAsyncData`/`useFetch`
- **影响**：SSR 渲染时 `init()` 是"发射后不管"，服务端吐给爬虫和用户的首屏 HTML **只有 loading 骨架，没有一篇文章**。同时客户端 hydration 时 setup 会再跑一遍 → **同一个接口请求两次**。首页是权重最高的页面，等于把首页 SEO 让出去了。
- **建议**：改成 `await useAsyncData('home-articles', () => fetchArticles(...))`，让 Nuxt payload 自动去重服务端/客户端请求。

### 31. ✅ 全站 `mousemove` 直接写响应式数据 → 整页重渲染

- `layouts/default.vue:63-64` → `window.addEventListener('mousemove', e => { glowStyle.value = ... })`
- **影响**：`default.vue` 是**包裹全站所有页面**的 layout，鼠标每移动一像素就触发一次 Vue 重渲染和样式重算 → 全站级卡顿。
- **建议**：用 `requestAnimationFrame` 节流，或者干脆改成直接操作 DOM（`el.style.left = ...`）不走响应式。

### 32. ✅ 滚动监听同样没节流

- `layouts/default.vue:66-71`（阅读进度条）、`components/Header.vue:188-196`（回到顶部按钮）
- **影响**：长文阅读时滚动会频繁重渲染，掉帧。
- **建议**：包一层 `requestAnimationFrame`。

### 33. ✅ `/post/{slug}` 整页没有任何 SEO meta

- `pages/post/[slug].vue` 全文没有 `useSeoMeta` / `useHead`
- **影响**：这本来是"最规范"的文章 URL，结果 `<title>` 退回全局默认值、无 description、无 og 标签 → 收录和分享效果都受损。
- **建议**：补 `useSeoMeta`（title/description/ogImage）。

### 34. ✅ 全站没有 `robots.txt` 和 `sitemap.xml`

- `blog-web/public/` 下只有 favicon、apple-touch-icon、vendor，**没有 robots.txt / sitemap.xml**
- 后端虽有 `SitemapController`，但没有机器人指引文件指向它
- **影响**：搜索引擎没有收录指引，新文章只能靠自然爬取，收录慢。
- **建议**：加 `public/robots.txt` 并在其中声明 sitemap 地址；或用 `@nuxtjs/sitemap` 生成。

### 35. ✅ 打开每篇文章都要拉一次"全部文章"来算上下篇

- `pages/article/[id].vue:316-324` → `fetchNeighbors()` 调 `/article/list?pageSize=100`
- **影响**：为了算出上一篇/下一篇的标题，每次打开文章都请求 100 条文章数据（服务端+客户端各一次），随文章数增长越来越慢。
- **建议**：后端加一个相邻文章接口，或复用列表缓存。

### 36. 评论/相关推荐/上下篇在客户端会重复请求一遍

- `pages/article/[id].vue:334` 等顶层直接调用 `fetchRelated()`，没用 `useAsyncData`
- **影响**：服务端已拉过的数据，客户端 hydration 时再拉一次，浪费流量。
- **建议**：统一包进 `useAsyncData('related-'+id, ...)`。

### 37. 缺 `canonical` → 同一篇文章两个 URL 被当成重复内容

- 全站没有任何 `canonical` 标签；`/post/{slug}` 与 `/article/{id}` 并存且内部链接混用（`ArticleCard.vue:2`）
- **影响**：两个 URL 相互竞争关键词，权重被稀释，搜索引擎可能只收录其中一个。
- **建议**：两页都加 `canonical` 指向规范 URL；内部链接统一优先 `/post/{slug}`；必要时给 `/article/{id}` 加 `noindex`。

### 38. 缺 `og:image` 与结构化数据

- `pages/article/[id].vue:336-341` 只设了 title/description/ogTitle/ogDescription
- **影响**：分享到微信/社交平台没有封面图；搜索引擎拿不到 Article 结构化数据（作者、发布时间、封面）。
- **建议**：用 `article.cover` 作 `ogImage`；注入 `application/ld+json` 的 Article schema。

### 39. 列表页/关于页/归档页普遍缺 description

- `pages/index.vue:426`、`about.vue:67`、`archive.vue:38`
- **影响**：搜索摘要质量差、分享无描述无图。
- **建议**：每页补 `description` 和 `ogImage`（可用站点默认封面）。

### 40. mermaid 放在 `public/` 且没有预压缩

- `blog-web/public/vendor/mermaid/` 约 200 个文件、总体积 MB 级
- 代码用了运行时按需 `import()`（这点做得好），但 `nitro.compressPublicAssets` **只压缩构建产物，不压缩 `public/`**
- **影响**：含 mermaid 图表的文章首访要下载未压缩的大文件。
- **建议**：nginx 对 `/vendor/` 开启 gzip/brotli 预压缩；或改用 npm 引入让构建期分包+加哈希。

### 41. 冗余依赖

- `blog-web/package.json` 里的 `mermaid` 从未被 import（运行时用的是 `public/vendor` 那份拷贝）
- `sharp` 是 `devDependency` 但项目没装 `@nuxt/image`，用不上
- `@nuxt/icon` 注册了但全项目没用过 `<Icon>`
- `nuxt.config.ts:30` 的 `manualChunks` 把 `highlight.js` 全量包和 `lib/common` 并列（代码只引 `lib/common`）
- **影响**：安装和构建体积、时间被无谓拉长。
- **建议**：确认后逐个移除。

---

## 🧱 第四批：数据完整性 与 工程质量

### 42. `article` 表缺关键复合索引

- `blog-server/sql/blog.sql:55-79`
- 列表查询固定过滤 `status + visibility + is_deleted` 并按 `is_top, create_time` 排序，但只有 `idx_create_time` / `idx_category` / `idx_user`
- **影响**：数据量上来后每次翻页都要 filesort / 全表扫描。
- **建议**：`KEY idx_list (status, visibility, is_deleted, is_top, create_time)`。

### 43. `article_tag` 缺 `tag_id` 单列索引

- `blog-server/sql/blog.sql:82-86`，主键是 `(article_id, tag_id)`
- **影响**：按标签查文章命不中索引，走全表扫描。
- **建议**：加 `KEY idx_tag (tag_id)`。

### 44. ✅ `listTags()` 把整张表拉进内存统计

- `ArticleService.java:245-262`：先 `selectList(null)` 取全部标签，再取全部已发布文章 id，再取全部 `article_tag`，然后在 Java 里 `groupingBy` 计数
- **影响**：每次访问 `/api/article/tags` 都把 `article`、`article_tag` 两张表**全量**载入内存。
- **建议**：一条 SQL 解决：`SELECT tag_id, COUNT(*) FROM article_tag JOIN article ON ... WHERE status=1 AND is_deleted=0 GROUP BY tag_id`。

### 45. 分类列表存在 N+1 查询

- `CategoryController.java:29-44`（`countArticles`）每个分类递归一次 `selectCount`
- **影响**：分类多时查询次数线性增长，且 `/category/list` 与 `/tree` 各跑一遍。
- **建议**：一次 `GROUP BY category_id` 统计完。

### 46. 全库没有外键，删除时不清理关联 → 孤儿数据

- `blog-server/sql/blog.sql` 无任何 FK；`CategoryController.java:91-95` 删分类不处理 `article.category_id`；删文章不清 `comment` / `article_tag`
- **影响**：引用完整性无保障，出现孤儿评论、孤儿标签关联、分类计数失真。
- **建议**：加 FK（或用软删除+定时清理）；至少删分类时把文章归类置空/转移，删文章时清掉评论和标签关联。

### 47. OAuth 三个 openId 字段没有唯一约束

- `blog-server/sql/oauth-alter.sql:2-4`（`github_id`/`gitee_id`/`huawei_id` 均无 `UNIQUE`）
- **影响**：并发回调可能给同一个第三方账号创建重复用户，导致登录串号。
- **建议**：三列加 `UNIQUE KEY`（MySQL 允许多行 NULL）。

### 48. 写接口普遍缺参数校验

- `CategoryController.java:73-80`、`TagController.java:27-33`、`FriendLinkController.java:30-33`、`SiteConfigController.java:42-58` 直接 `@RequestBody` 绑实体，无 `@Valid`
- 评论接口同样无长度/频率校验（见第 10 条）
- **建议**：引入 DTO + `@Valid`（`@NotBlank` / `@Size`）。

### 49. 启动期偷偷 `ALTER TABLE`，失败只打日志

- `WordCountMigrator.java:42-44`：启动时自动补列，失败仅 `log.warn`，应用照常启动
- **影响**：数据库列缺失时应用启动"成功"，直到第一次查文章才报 `Unknown column` 500。
- **建议**：迁移失败应 fail-fast 或告警；中长期建议引入 Flyway/Liquibase 管理 schema（现在 `word_count`、`huawei_id` 都靠运行时补列，很脆弱）。

### 50. ✅ nginx 没有 `/uploads` 的反向代理 → 上传的图线上 404

- `deploy/nginx.conf` 只有 `location /`（转发给 Nuxt），`WebMvcConfig.java:15-18` 映射的 `/uploads/**` 到不了 Java
- **影响**：本地开发能看到的图片，部署后 404。Spring 侧那个资源处理器等于死代码。
- **建议**：nginx 加 `location /uploads/ { alias /opt/blog/uploads/; }`（并限制返回的 Content-Type）。

### 51. 部署脚本用相对路径、无备份无回滚

- `deploy/deploy.sh:13,19` → `cd ../blog-server`、`cd ../blog-web`
- **影响**：脚本必须在特定目录执行，且覆盖前不备份、失败后无法回滚。
- **建议**：改绝对路径；部署前备份旧 jar 和静态资源；加 `set -euo pipefail`。

### 52. 排序逻辑有 bug

- `ArticleService.java:69` → `ORDER BY CAST(title AS UNSIGNED) ASC`
- **影响**：`sort=title` 时，非纯数字标题的 `CAST` 结果是 0，排序结果错乱。
- **建议**：直接 `ORDER BY title`。

### 53. 阅读量每次访问同步 `UPDATE` + 回查

- `ArticleService.java:120-128`（`getPublicById`）即使命中 Redis 缓存也照样 `UPDATE view_count = view_count + 1` 再回查一次
- **影响**：热点文章写放大，数据库压力集中在这一个字段上。
- **建议**：Redis `INCR` 计数，定时批量落库。

### 54. 归档 / 系列接口全量拉内存聚合

- `ArticleController.java:247-315`（`archive` / `series`）`selectList` 所有已发布文章后在内存里分类汇总，无分页无缓存
- **影响**：文章数增长后响应体和 DB 扫描同步膨胀。
- **建议**：加缓存（或 Redis），或按年份分批。

### 55. CI 还能加几道门

- `.github/workflows/ci.yml`：目前只跑后端单测 + 前端单测与构建
- **建议**：
  - 前端加 `npx nuxi typecheck`（Nuxt build **不会**因为类型错误失败，现在 `any` 泛滥没人挡）
  - 加 `concurrency` 组取消旧运行、缓存 `node_modules`
  - 加依赖漏洞扫描（OWASP dependency-check / `npm audit`）、密钥扫描
  - 加 RAG 子系统的 `pytest`
  - 升级 `actions/checkout@v5`、`actions/setup-java@v5`（消除 Node 20 弃用告警）

---

## 📌 建议的实施批次

| 批次 | 内容 | 理由 |
| --- | --- | --- |
| **第 1 批（半天）** | 第 1～12 条中的 1、3、4、6、7、9、11 + 第 50 条 | 都是"改几行就能消除的事故/泄露风险"，其中**第 1 条（部署不生效）和第 3、4 条（私密文章泄露）最优先** |
| **第 2 批（1 天）** | 第 13～17、25、26 + 第 30、31 条 | 用户可感知：后台手机可用、404 页不再诡异、深色模式正确；外加首页 SSR 与全站卡顿这两个大头 |
| **第 3 批（1 天）** | 第 2、5、8、12、18～24、27～29 条 | RAG 加固、上传白名单、XSS 净化、弹窗/主题色/响应式等体验统一 |
| **第 4 批（1~2 天）** | 第三、四批的其余项 | 性能优化、SEO、索引与数据完整性、CI 加强 |

---

## 附录：本次会话已修复（不在待办内）

- 列表查询未加列白名单，导致每页读回 15 篇正文（`content_md` / `content_html`）
- `related()` 相关推荐接口响应体里含 4 篇完整文章 HTML
- 代码高亮主题被构建管线整个丢弃（`@import` 位置非法）
- `error.vue` 在生产环境渲染完整调用堆栈
- CI 因 `.nuxt/tsconfig.json` 缺失而一直失败
- 后台列表接口返回 `password` 字段（**详情接口仍存在，见第 9 条**）
