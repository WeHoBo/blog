# 好啵博客 · 新功能建议清单

> 视角：软件工程 + 产品。基于对现有代码的**实际盘点**（不是凭空设想）提出建议。
> 每条都标了：用途 / 目标用户 / 使用场景 / 与现有功能的关联 / 实现难度 / 预期价值 / 优先级。
> **本文只做建议，未改动任何代码。**

---

## 0. 先看清楚现状：哪些"地基"已经打好了

这一步很关键——**有些功能只差一层 UI**，而有些要从零建表建接口，两者的性价比差一个数量级。

### 0.1 已有能力基线

| 层 | 已有 |
|---|---|
| 读者端 | 首页（分类/标签筛选、排序、关键词搜索）、文章详情（目录、代码高亮、mermaid、AI 总结、划词解释、评论、上下篇、相关推荐）、归档、关于 + 友链、主题设置（主题色/暗色/背景色）、OAuth 登录 ×3、个人中心 |
| AI | 右下角悬浮助手：**RAG 检索问答**（带 sources 溯源）+ **自由问答**（解释/总结），流式输出 |
| 后台 | 文章 CRUD（导入 md/Word、导出、批量、置顶、slug、定时字段）、版本历史、回收站、分类、标签、友链、用户、操作日志、站点设置 |
| 基础设施 | Redis 缓存、图片上传（OSS）、`sitemap.xml`、独立 RAG 服务（FastAPI + ChromaDB + DeepSeek，**支持 PDF 灌库**） |

### 0.2 ⭐ 已存在但"没接上"的部分（**低成本高确定性**，最该先做）

| 已经有的东西 | 缺什么 | 意味着 |
|---|---|---|
| `article.like_count` 字段 | 无点赞接口、无 UI | 点赞只差一层皮 |
| `article.series` 字段 + `/api/article/series` 聚合接口 | 前端**完全没呈现** | 系列功能已就绪一半 |
| `comment.parent_id` 字段 | 后端无回复接口、前端平铺展示 | 楼中楼只差接口 + UI |
| `comment.status` 字段 | 无审核队列（当前自动通过） | 审核只差后台页 |
| `article.publish_at` 字段 | 定时发布的完整逻辑/提示 | 定时发布半成品 |
| 搜索 `keyword` 参数 | **只 `LIKE` 标题**，不搜正文/标签 | 搜索功能名不副实 |
| `site_config` 是 **Key-Value 表** | —— | **加任何新设置项都零数据库迁移** |
| RAG `/ingest-articles`（文章灌库）、PDF 上传 | 博客侧无入口 | 知识库能力**完全闲置** |
| RAG `/history`（问答历史） | 前端未使用，刷新即丢 | 会话持久化只差调用 |
| 独立 `/ai` 页面 | 已废弃为跳转页，无任何入口 | 「AI 问答」入口缺失 |

### 0.3 一个必须点出的"产品漏洞"

首页搜索框的 placeholder 写着 **「搜索文章、标签、分类 (Ctrl+K)」**，但实际搜索只对 `title` 做 `LIKE`，**不搜正文、不搜标签、不搜分类**。这是对用户的**虚假承诺**——要么兑现，要么改文案。

---

## 1. 建议清单（按主题分组）

难度评级：**低**（1 人日内 / 主要前端）· **中**（需前后端 + 可能建表）· **高**（跨系统 / 需新架构）
价值评级：以"对**读者体验**或**站长长期收益**的实际提升"衡量。

---

### A 组 · 把"半成品"补完（性价比最高，建议第一批）

#### A1. 文章点赞（带"已赞"去重）
- **用途**：给读者一个比评论更轻的反馈方式，同时为站长提供"哪篇真的受欢迎"的信号。
- **目标用户**：所有读者（尤其是懒得评论的人）。
- **使用场景**：读完一篇干货，想表达"有用"但不想打字；或收藏夹式的"我记住这篇"。
- **与现有功能关联**：`article.like_count` 字段**已在库**；可直接参与首页/后台的排序（现在只有 `viewCount` 和 `isTop`）。
- **实现难度**：**低**。只需 1 张 `article_like(user_id, article_id, 唯一索引)` 表 + 点赞/取消接口 + 详情页按钮。
- **预期价值**：中高。**唯一索引**保证"一人一赞"，数据比"阅读数"干净得多（阅读数会被刷新灌水）。
- **优先级**：**P0**

#### A2. 评论楼中楼 + 评论审核队列
- **用途**：让讨论真正发生（现在只能各自发一条），并把"垃圾评论"挡在门外。
- **目标用户**：读者（回复）、站长（审核）。
- **使用场景**：读者 A 提问 → 读者 B 或站长**在原评论下回复**；站长在后台批量通过/驳回。
- **与现有功能关联**：`comment.parent_id` 与 `comment.status` **两个字段都已存在**，后端只差 `/reply` 与 `/audit` 两个接口，前端把平铺列表改成两层缩进即可。
- **实现难度**：**中**（后端 2 个接口 + 后台 1 个页面 + 前端嵌套渲染）。
- **预期价值**：高。评论是博客唯一"活人感"，楼中楼让问答可追踪，审核则决定站长是否敢开放评论。
- **优先级**：**P0**

#### A3. 文章系列（Series）页
- **用途**：把"零散文章"变成"成体系的教程"，这是技术博客最核心的读者留存手段。
- **目标用户**：想系统学习的读者；想沉淀知识体系的站长。
- **使用场景**：读《Spring Boot 从 0 到 1》第 3 篇时，侧栏显示**整个系列 12 篇的目录**，可点进第 4 篇；系列页 `/series/{name}` 汇总全集。
- **与现有功能关联**：`article.series` 字段 + `/api/article/series` 聚合接口**都已就绪**，前端**一点没做**——目前只能靠 admin 手填系列名，读者完全看不到。
- **实现难度**：**低–中**（主要是前端：详情页系列导航块 + 系列落地页，SSR 友好）。
- **预期价值**：**高**。对技术博客来说，"系列"的留存价值通常**高于**单篇推荐。
- **优先级**：**P0**

#### A4. 真正的全文搜索（正文 + 标签 + 分类 + 高亮）
- **用途**：兑现搜索框对用户的承诺，让老内容能被翻出来。
- **目标用户**：所有读者（含站内二次查找的站长）。
- **使用场景**：想找"之前写过 Redis 缓存击穿那段代码"，搜「缓存击穿」能命中**正文**，结果页高亮关键词并显示上下文。
- **与现有功能关联**：`keyword` 参数与 `/article/list` 已存在，只是 SQL 从 `title LIKE` 扩到 `title/summary/content` + 标签关联；`site_config` 是 KV 表，可加"每页搜索条数"等设置。
- **实现难度**：**中**。方案二选一：① MySQL `FULLTEXT` 索引（轻，但中文分词弱，需 `ngram` 解析器）；② 复用**已有的 ChromaDB 向量库**做语义搜索（重，但效果更好，且**基础设施已存在**）。
- **预期价值**：高。搜索是内容型网站的第二大入口。
- **优先级**：**P0**（先用方案①，快）→ **P1**（再升级语义搜索）

#### A5. 定时发布"看得见"
- **用途**：让站长能排期发布，且不会误以为没设置成功。
- **目标用户**：站长（唯一作者）。
- **使用场景**：周日晚上写好 3 篇，排到周一/三/五 09:00 自动发布。
- **与现有功能关联**：`article.publish_at` 字段已在；后台列表状态列目前只有"草稿/已发布"，定时文章显示为"草稿"，**看不出排期**。
- **实现难度**：**低**（后台列表加"定时发布 + 具体时间"，编辑页做成日期选择器）。
- **预期价值**：中。对个人博客是"省心"，不是"必须"。
- **优先级**：**P1**

---

### B 组 · AI 纵深（这是本项目**最独特的护城河**，别浪费）

> 项目里已经有一套**手写的 RAG（FastAPI + ChromaDB + DeepSeek）**，还支持 PDF 灌库。这在国内个人博客里相当少见。但目前只用了"悬浮窗问答"一个场景，**90% 的能力闲置**。

#### B1. 站内知识库问答页（把 RAG"正名"）
- **用途**：让访客能"和这个博客对话"，而不只是翻文章。
- **目标用户**：新访客（快速了解博主擅长的领域）、老读者（检索式提问）。
- **使用场景**：访客问"你这里有没有讲过 JWT 无状态登出？"→ 系统检索站内文章并**带原文出处链接**回答。
- **与现有功能关联**：RAG 的 `/chat` + `/ingest-articles`（已能把文章灌进向量库）**都已存在**，缺的只是一个正式入口页（现在 `/ai` 已废弃为跳转）。
- **实现难度**：**低**（主要是前端页面 + 把 sources 渲染成可点跳转；后端已通）。
- **预期价值**：高。既是功能，也是**技术展示面**（面试/接单时的活广告）。
- **优先级**：**P0**

#### B2. 「AI 速读」——摘要落库 + 文章顶部常驻
- **用途**：长文读者 3 秒判断"值不值得读"；同时提升首屏信息密度。
- **目标用户**：时间紧张的读者。
- **使用场景**：打开 5000 字长文，顶部直接给"核心结论 / 关键知识点 / 预计阅读 8 分钟"。
- **与现有功能关联**：现在"AI 总结"是**点一下才生成、且只在悬浮窗里**，每次都要**重新烧 token**；改为**生成后写回 `article.summary` 之外的新字段并缓存**，所有人共享。
- **实现难度**：**中**（新增字段/缓存 + 后台"生成摘要"按钮 + 前台展示）。注意：`site_config` 是 KV，但文章级缓存需要新列或单独表。
- **预期价值**：中高。**省 token** 本身就是价值（现在是每读一次烧一次）。
- **优先级**：**P1**

#### B3. 语义相似推荐（替换现在的"同分类相关推荐"）
- **用途**：让"相关推荐"真的相关。
- **目标用户**：读完一篇想继续读的读者。
- **使用场景**：读完《Redis 缓存击穿》，推荐的是同样讲缓存的另外 3 篇——**哪怕它们不在同一分类**。
- **与现有功能关联**：现有 `/api/article/{id}/related` 大概率是"同分类取几条"，且前端跳转 `/article/{id}`（还制造了重复 URL）；换成向量相似度后，推荐质量质变。
- **实现难度**：**中–高**（写入文章时同步灌向量 + 查询相似 + 回填文章 ID；依赖 RAG 服务**与后端同机**）。
- **预期价值**：高。**相关系数是内容站留存的关键指标**。
- **优先级**：**P1**

#### B4. 编辑器里的"AI 助手副驾"
- **用途**：把写作里最费时的机械活自动化。
- **目标用户**：站长。
- **使用场景**：写完一篇，点「生成」→ 自动产出 **摘要、SEO 描述、标签建议、slug 建议**；一键采纳。
- **与现有功能关联**：`summary`、`slug`、`tag` 字段都在；`slug` 唯一性预检接口 `/slug-available` 已有；`/ai/free` 可复用。
- **实现难度**：**中**（编辑器面板 + 3–4 个复用 AI 的按钮 + 写回表单）。
- **预期价值**：中高。**直接减少站长每篇的边际工时**。
- **优先级**：**P1**

#### B5. 划词工具的"段落级追问"
- **用途**：把现在的一次性"解释这段"升级成可对话的学习助手。
- **目标用户**：学习型读者。
- **使用场景**：选中一段代码 → 解释后**继续追问**"如果并发调用会怎样？"，且上下文不丢。
- **与现有功能关联**：`/ai/free` 的 `explain` 模式已实现单向解释；缺"围绕选区**多轮**"的会话承载（RAG 侧 `/history` 可复用）。
- **实现难度**：**中**（前端会话状态 + 把选区块作为固定上下文）。
- **预期价值**：中。偏"锦上添花"，但**对技术博客的读者黏性提升明显**。
- **优先级**：**P2**

#### B6. 「AI 会话持久化 + 服务端历史」
- **用途**：刷新页面不丢对话；换设备能接着问。
- **目标用户**：使用 AI 助手的读者。
- **使用场景**：看到一半去吃饭，回来接着问。
- **与现有功能关联**：RAG 的 `/history`（SQLite 存问答）**已实现**，前端却在用内存 `useState`，刷新即丢。
- **实现难度**：**低**（前端接一下现成接口）。
- **预期价值**：中。**接近零成本的体验补齐**。
- **优先级**：**P1**

---

### C 组 · 创作者效率（站长是唯一用户，但天天用）

#### C1. 后台数据看板（Dashboard）
- **用途**：让站长一眼看到"站点在长还是在停"。
- **目标用户**：站长。
- **使用场景**：周一早上看到"本周 +3 篇、阅读 +18%、评论 12 条、最热是《…》"，据此决定下一篇写什么。
- **与现有功能关联**：后台**目前没有任何概览页**（`/admin` 直接是文章列表）；`view_count`/`comment_count`/`like_count`（A1 后）都在库；`operation_log` 可用于"最近改动"。
- **实现难度**：**中**（聚合接口 + 图表；数据量小，直接 SQL 聚合够用）。
- **预期价值**：高。**没有反馈的写作很难坚持**——这是"长期动力"层面的功能。
- **优先级**：**P1**

#### C2. 编辑器增强：图片粘贴直传 + Markdown 片语
- **用途**：把"插图"从"存本地→上传→复制链接→粘回"4 步降到 1 步。
- **目标用户**：站长。
- **使用场景**：截图后直接在编辑器里 `Ctrl+V`，图片自动传到 OSS 并插入 `![](url)`。
- **与现有功能关联**：`/upload` 接口与 OSS 已就绪（现在只能靠"上传头像/背景图"那种按钮式入口）。
- **实现难度**：**低**（编辑器粘贴事件 + 复用现成上传接口）。
- **预期价值**：**中高**。写作频率越高，收益越大。
- **优先级**：**P1**

#### C3. 「内容体检」：批量 SEO / 质量巡检
- **用途**：找出自己博客里"漏掉的细节"。
- **目标用户**：站长。
- **使用场景**：一键列出：缺 `slug` 的文章、缺摘要的、标题过长的、正文没有小标题（无目录）的、内外链死链、图片缺 `alt` 的。
- **与现有功能关联**：`slug`/`summary`/`cover`/目录（`heading` 解析）都在；`SitemapController` 已证明能遍历全站文章。
- **实现难度**：**中**（巡检接口 + 后台表格 + 一键跳转修复）。
- **预期价值**：中高。**对 SEO 有实打实的长期收益**，且是"批处理型"的低频高价值工具。
- **优先级**：**P1**

#### C4. 版本历史的**可视化 diff**
- **用途**：回滚前先看清"这版到底改了什么"。
- **目标用户**：站长。
- **使用场景**：想回滚到 3 天前，先看 diff 确认没有误删段落。
- **与现有功能关联**：`ArticleRevision` 与版本列表页**已存在**，但只能看整篇内容，**没有左右对比/差异高亮**。
- **实现难度**：**中**（前端 diff 渲染，可用现成 diff 算法；后端已有数据）。
- **预期价值**：中。回滚是低频高风险操作，diff 能实质降低"越改越乱"的风险。
- **优先级**：**P2**

---

### D 组 · 读者体验与增长

#### D1. RSS / Atom 订阅
- **用途**：让老读者"追更"，不依赖算法推荐。
- **目标用户**：技术读者（RSS 在技术圈**至今存续**）。
- **使用场景**：读者把博客加进 Feedly/Follow，新文自动推送。
- **与现有功能关联**：`SitemapController` 已有"遍历全站文章"的实现，**照同一个模式加 `/rss.xml` 即可**；页脚目前只有版权和备案号，是天然的入口位。
- **实现难度**：**低**（一个 Controller + 页脚一个图标）。
- **预期价值**：中高。**一次开发，长期自动获客**，且 SEO 之外多一条分发渠道。
- **优先级**：**P1**

#### D2. 「稍后读」清单 + 阅读进度续读
- **用途**：让长文读者不必一次读完，也愿意回来。
- **目标用户**：通勤/碎片时间阅读的读者（**移动端为主**）。
- **使用场景**：地铁上读了 1/3，收藏进"待读"，晚上从上次位置接着读。
- **与现有功能关联**：登录体系与 `localStorage` 都在；可先做**纯前端本地版**（零后端）。
- **实现难度**：**低**（本地版）→ **中**（跨设备需后端表）。
- **预期价值**：中。移动端留存提升明显。
- **优先级**：**P2**

#### D3. 标签页 / 分类页独立落地页
- **用途**：每个标签/分类都能被搜索引擎独立收录。
- **目标用户**：从搜索引擎进来的新访客。
- **使用场景**：搜"Spring Boot 教程"命中站内 `/tag/spring-boot` 聚合页。
- **与现有功能关联**：现在标签/分类都跳回首页 `/?tagId=x`，**没有独立 URL、没有独立 title/description**，首页 canonical 又恒指向首页 → **属于 SEO 上的"重复 URL 稀释"**。
- **实现难度**：**低–中**（新增两个动态路由页 + 各自的 SEO meta）。
- **预期价值**：**高**。这是纯 SEO 收益，**成本极低、回报持续**。
- **优先级**：**P0**

#### D4. 分享卡片（自动生成 OG 图 / 长图）
- **用途**：让分享到微信/即刻/微博时"有图有面"。
- **目标用户**：愿意转发好文的读者；站长。
- **使用场景**：读者点"生成卡片"，得到一张带标题+作者+二维码的图，直接转发。
- **与现有功能关联**：`useSeoMeta` 已在做 OG 标签（但图是固定的 `apple-touch-icon.png`）；可服务端生成动态 OG 图，或前端 canvas 生成长图。
- **实现难度**：**中**（前端 canvas 版较易；服务端动态 OG 图较重）。
- **预期价值**：中高。**传播效率直接决定博客的"出圈"概率**。
- **优先级**：**P2**

#### D5. 「写作热力图」（GitHub 风格贡献图）
- **用途**：把"坚持写作"可视化，对站长是强激励。
- **目标用户**：站长（也可展示在关于页激励访客）。
- **使用场景**：关于页显示"过去一年写下的 87 个格子"。
- **与现有功能关联**：按 `create_time` 聚合文章数即可；`/archive` 接口已经在做类似聚合。
- **实现难度**：**低**（纯前端渲染 + 一个聚合接口）。
- **预期价值**：中（**情绪价值 > 功能价值**，但对个人博客的"坚持"很有效）。
- **优先级**：**P2**

#### D6. 友链自助申请
- **用途**：把"换友链"从私聊变成流程。
- **目标用户**：其他站长（潜在互推伙伴）。
- **使用场景**：对方填表提交站点信息 → 站长在后台一键通过 → 自动出现在关于页。
- **与现有功能关联**：`FriendLink` 实体 + 后台 CRUD **已有**，只差"前台提交入口 + 待审核状态"。
- **实现难度**：**低**（表单 + 复用一个新状态字段；`status` 模式下零迁移）。
- **预期价值**：中高。**友链是个人博客最廉价的外链来源**。
- **优先级**：**P1**

#### D7. 站内通知（"你的评论被回复了"）
- **用途**：把"被回复"这件事告诉读者，形成回访。
- **目标用户**：评论过的读者。
- **使用场景**：站长回复了 3 天前的提问，读者收到站内信并回来继续讨论。
- **与现有功能关联**：依赖 A2（回复）先落地；登录体系已有。
- **实现难度**：**中**（通知表 + 未读数 + 顶部铃铛）。
- **预期价值**：中。**在 A2 之后才有意义**。
- **优先级**：**P2**（依赖 A2）

---

### E 组 · 平台化 / 工程纵深

#### E1. 多作者与投稿流程
- **用途**：从"个人博客"走向"小团队技术专栏"。
- **目标用户**：想聚合朋友一起写的站长；投稿者。
- **使用场景**：朋友投稿 → 站长审核 → 发布（作者归属正确显示）。
- **与现有功能关联**：`article.user_id` 与作者信息渲染**已支持**（详情页有作者卡片）；`User.role` 已有 `admin` 概念，扩一个 `author` 即可。
- **实现难度**：**高**（权限模型、投稿状态机、后台审核流）。
- **预期价值**：中（**取决于你是否真的想要多人协作**，单人博客做了反而增加复杂度）。
- **优先级**：**P2**（想清楚再动）

#### E2. PWA / 离线阅读
- **用途**：手机端"添加到桌面"、弱网可读。
- **目标用户**：移动端回头读者。
- **使用场景**：地铁信号差，之前读过的文章仍能打开。
- **与现有功能关联**：Nuxt 3 有成熟 PWA 模块，SSR 站点接入成本不高。
- **实现难度**：**中**（Service Worker 缓存策略要小心**别把 SSR 页面缓存坏**）。
- **预期价值**：中。
- **优先级**：**P2**

#### E3. 自建访问统计（替代第三方）
- **用途**：不依赖第三方，掌握自己的数据。
- **目标用户**：站长。
- **使用场景**：看"哪些文章被搜进来了""跳出率""来源"。
- **与现有功能关联**：`view_count` 已有但是粗粒度；`operation_log` 已证明"记录行为"的模式可行。
- **实现难度**：**中–高**（埋点 + 聚合 + 隐私合规 + 存储增长）。
- **预期价值**：中（**如果已有第三方统计，收益不大**）。
- **优先级**：**P2**

---

## 2. 排序：难度 × 价值矩阵

```
价值
高 │  A3 系列页        B1 知识库问答      B3 语义推荐
   │  D3 标签落地页    A2 评论楼中楼
   │  A4 全文搜索                         C1 数据看板
   │  A1 点赞          D1 RSS
中 │  B6 会话持久化    C2 图片粘贴直传    B4 AI 副驾
   │  D6 友链申请      A5 定时发布        B2 摘要落库
   │  C4 diff                        D7 站内通知
   │  D5 热力图        D4 分享卡片      B5 段落追问
   │                              E1 多作者    E3 自建统计
低 │                            E2 PWA
   └──────────────────────────────────────────────────
      低                     中                     高      难度
```

### 综合排序表（推荐执行顺序）

| 排名 | 功能 | 难度 | 价值 | 优先级 | 一句话理由 |
|---|---|---|---|---|---|
| 1 | **A1 文章点赞** | 低 | 中高 | **P0** | 字段已在库，一张表 + 一个按钮，立刻可用 |
| 2 | **A3 系列页** | 低–中 | 高 | **P0** | 字段 + 聚合接口都已就绪，纯前端就能出效果 |
| 3 | **D3 标签/分类落地页** | 低–中 | 高 | **P0** | 纯 SEO 收益，成本极低、长期复利 |
| 4 | **B1 知识库问答页** | 低 | 高 | **P0** | RAG 已通，只缺入口；同时是最强"技术展示面" |
| 5 | **A2 评论楼中楼 + 审核** | 中 | 高 | **P0** | 两个字段已存在，讨论才能真正发生 |
| 6 | **A4 全文搜索** | 中 | 高 | **P0→P1** | 兑现搜索框承诺（先 MySQL FULLTEXT） |
| 7 | **B6 AI 会话持久化** | 低 | 中 | **P1** | 调用现成 `/history`，近乎零成本 |
| 8 | **C2 图片粘贴直传** | 低 | 中高 | **P1** | 复用现成上传接口，写作效率立竿见影 |
| 9 | **D1 RSS 订阅** | 低 | 中高 | **P1** | 照 sitemap 的模式写，页脚加个图标 |
| 10 | **D6 友链自助申请** | 低 | 中高 | **P1** | FriendLink 后台已有，只缺前台入口 |
| 11 | **A5 定时发布可见化** | 低 | 中 | **P1** | 字段已在，后台加分状态即可 |
| 12 | **C1 数据看板** | 中 | 高 | **P1** | 影响"能不能长期写下去"的反馈闭环 |
| 13 | **B3 语义相似推荐** | 中–高 | 高 | **P1** | 相关系数决定留存，且能顺带修掉重复 URL |
| 14 | **B4 编辑器 AI 副驾** | 中 | 中高 | **P1** | 直接降低站长每篇的边际工时 |
| 15 | **B2 AI 摘要落库** | 中 | 中高 | **P1** | 现在每次阅读都在重复烧 token |
| 16 | **C3 内容体检** | 中 | 中高 | **P1** | 批处理型工具，SEO 长期收益 |
| 17 | **D5 写作热力图** | 低 | 中 | **P2** | 情绪价值高，实现很便宜 |
| 18 | **C4 版本 diff** | 中 | 中 | **P2** | 降低"回滚翻车"风险 |
| 19 | **D4 分享卡片** | 中 | 中高 | **P2** | 传播效率，可先做前端 canvas 版 |
| 20 | **D2 稍后读/续读** | 低→中 | 中 | **P2** | 先做本地版，零后端成本 |
| 21 | **B5 段落级追问** | 中 | 中 | **P2** | 锦上添花，但读者黏性有提升 |
| 22 | **D7 站内通知** | 中 | 中 | **P2** | 依赖 A2，需排在评论之后 |
| 23 | **E1 多作者投稿** | 高 | 中 | **P2** | 想清楚是否真要协作，否则纯增复杂度 |
| 24 | **E2 PWA 离线** | 中 | 中 | **P2** | Service Worker 与 SSR 缓存要小心 |
| 25 | **E3 自建统计** | 中–高 | 中 | **P2** | 已有第三方统计则不划算 |

---

## 3. 建议的落地批次

| 批次 | 内容 | 特点 |
|---|---|---|
| **第 1 批 · 低成本补齐**（1–2 周） | A1 点赞、A3 系列页、D3 标签落地页、B1 知识库问答页、B6 会话持久化、D5 热力图 | 全是"地基已打好、只差 UI/一两个接口"，**风险低、见效快** |
| **第 2 批 · 社区与增长**（2–3 周） | A2 评论楼中楼 + 审核、D1 RSS、D6 友链申请、A4 全文搜索、D7 站内通知 | 让读者从"看"变成"参与"，同时打开分发渠道 |
| **第 3 批 · AI 纵深**（3–4 周） | B2 摘要落库、B3 语义推荐、B4 编辑器副驾、B5 段落追问 | 依赖 RAG 服务稳定；**注意 token 成本** |
| **第 4 批 · 精益与平台**（按需） | C1 看板、C2 粘贴直传、C3 体检、C4 diff、D2 稍后读、D4 分享卡、E 组 | 按兴趣取舍，不必全做 |

---

## 4. 实现时的约束与影响（务必先读）

1. **点赞必须做"一人一赞"**：只加 `like_count` 自增会立刻被刷。需要 `(user_id, article_id)` 唯一索引，且**要能取消点赞**（否则用户误点无法撤销）。
2. **评论审核一旦开启，站长的日常负担就变了**：当前是"自动通过"，开了审核后每条都要点。建议**先做"自动通过 + 后台可删"**，稳定后再上审核队列——避免热情上来时把自己累垮。
3. **全文搜索的中文分词是个坑**：MySQL 默认 `FULLTEXT` 对中文几乎无效，必须用 `ngram` 解析器（`WITH PARSER ngram`）。否则搜"缓存"可能搜不到"缓存击穿"。
4. **AI 类功能要算成本**：B2/B3/B4 都会调用 DeepSeek。摘要**落库缓存**是省钱关键——否则"每个访客每读一次就烧一次 token"。
5. **语义推荐依赖 RAG 与后端同机**：RAG 绑 `127.0.0.1`，且要两侧都配 `RAG_API_TOKEN`。做 B3 前先确认这条链路稳定。
6. **新增站点设置项是零迁移的**：`site_config` 是 Key-Value 表，**加设置项不需要改表结构**——但前端必须真的读它，否则又会造出"改了没反应"的无效控件（这正是现存的问题）。
7. **`/series`、`/tag/xxx`、`/category/xxx` 新增路由时注意 canonical**：项目现有约定是"canonical 一律指向 `/post/{slug}`"。**聚合页要有自己的 canonical，不能继承文章页的规则**，否则等于告诉搜索引擎"别收录我"。
8. **E1 多作者是"产品决策"不是"技术任务"**：单人博客引入权限模型和状态机会显著增加维护成本。如果只是想"朋友偶尔发一篇"，用"站长代发 + 作者归属字段"就够，不必做整套投稿流。
9. **别一次做完**：审计清单里已明确"本机无法完整构建、没有 E2E 与视觉快照，全靠人工走查"。建议**一批只动 2–3 个功能**，每个都配自测清单，避免出问题时分不清是哪批引起的。
10. **PWA 与 SSR 共存要谨慎**：Service Worker 缓存 HTML 很容易导致"部署了新版本但用户看到旧的"，必须做版本化与失效策略。

---

## 5. 如果只做三件事

如果精力有限，我建议就做这三个——它们**投入最小、复利最大**：

1. **A3 文章系列页** —— 字段和接口全部就绪，纯前端就能落地，直接提升读者留存。
2. **D3 标签/分类独立落地页** —— 修掉现有的 SEO 缺陷，成本极低，长期持续带来搜索流量。
3. **B1 站内知识库问答页** —— RAG 都已建好，只缺一个入口；既服务访客，也是你技术能力最直观的证明。
