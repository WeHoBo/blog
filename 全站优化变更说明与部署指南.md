# 全站优化变更说明与部署指南（本地 → 服务器同步用）

> 生成时间：2026-09-11
> 变更范围：`5020218` 之后共 **9 个提交**、**65 个文件**（+2124 / −700）
> 适用：从 `main` 更新到服务器的部署操作

---

## 一、这次一共改了什么（总览）

| 批次 | 提交 | 主题 |
| --- | --- | --- |
| 前序 | `160860f` | 移除已下线的「私密文章」功能残留代码 |
| 第 1 批 | `36fa7a9` | 删除加密文章死字段 `is_encrypted` / `password` |
| 第 1 批 | `5ff6479` | 安全与线上事故级修复（部署 / 鉴权 / 反序列化 / 上传 / 评论 / OAuth / XSS） |
| 第 3 批 | `0a4ac87` | 数据层与后端查询优化（索引 / 聚合 / 校验 / 迁移 / 浏览量 / 缓存） |
| 第 2 批 | `5959192` | UI 与交互（后台移动端、弹窗体系、主题色、无障碍） |
| 第 4 批 | `5432d44` | 性能与 SEO（首页 SSR、canonical、JSON-LD、相邻文章接口、依赖清理） |
| 第 4 批 | `ab5a51b` | CI 强化（与部署无关） |
| 文档 | `f4b46ef` / `d643af1` | 审计清单 + 实施记录 |

### 影响面一览（决定你要做什么）

| 对象 | 是否需要动 | 说明 |
| --- | --- | --- |
| 后端 jar | ✅ **必须重新构建** | 20 个 Java 文件、`application.yml` 变更 |
| 前端产物 | ✅ **必须重新构建** | 25 个前端文件 + 3 个新增文件 + 依赖变更 |
| 数据库 | ⚠️ **需执行 1 个增量 SQL** | 仅补索引 / 唯一约束，不改表结构、不删数据 |
| 配置文件 | ⚠️ **需新增 1 个 key** | `ai.rag-token`（+ 可选 `app.cors.allowed-origins`） |
| nginx | ✅ 需同步 `nginx.conf` | 体积上限 / AI 流式超时 / `/uploads/` 反代 |
| systemd | ✅ 需同步 `blog-api.service` | jar 名已改（历史 bug） |
| RAG 子系统 | ⚠️ 需重启 + 配 token | 绑 `127.0.0.1`、关闭 reload、加调用令牌 |

---

## 二、必须知道的三件事（先看这个）

### 1. `deploy/` 已被 gitignore，`git pull` 不会更新部署文件

`deploy.sh` / `nginx.conf` / `*.service` / `application-prod.yml` **都不在版本库里**。
所以「服务器 `git pull`」只能拿到代码，部署脚本与配置必须从本机手工拷过去。

### 2. 修掉了一个「部署静默失效」的坑（两个）

- **后端**：`deploy.sh` 原本把 jar 复制成 `blog-api.jar`，而 `blog-api.service` 启动的是
  `blog-front-api-1.0.0.jar` → 脚本提示成功，实际跑的还是**上一次的旧 jar**。现已统一为
  `/opt/blog/blog-api.jar`。
- **前端**：旧脚本只把 `.output/public` 拷到 nginx 静态目录，但 `nginx.conf` 里**没有任何
  `root` / 静态目录引用**，所有请求都走 `127.0.0.1:3000` 反代 → 前端改动**从未真正上线**。
  现已改为部署整个 `.output` 到 `/opt/blog/web/.output` 并重启 `blog-web`。

### 3. RAG 的鉴权要两侧都配才生效

`rag_system/backend` 侧：若 `RAG_API_TOKEN` 为空 → **跳过校验**并打印警告（等于没防护）。
Java 侧：若 `ai.rag-token` 为空 → 不发 `X-RAG-Token` 头。
**只有两侧都配上同一个值才真正生效**；两边不一致会直接 401，AI 问答不可用。

---

## 三、部署步骤（照做）

### 步骤 0 —— 先把代码推上去（需要你授权）

本机 `main` 目前领先远端 9 个提交，**尚未 push**。授权后：

```bash
cd ~/Blog
git push origin main
```

### 步骤 1 —— 服务器上拉代码 + 备份

```bash
cd ~/Blog
git pull
# 备份旧产物，便于回滚
sudo cp /opt/blog/blog-api.jar /opt/blog/blog-api.jar.bak-$(date +%F)
sudo cp -r /opt/blog/web/.output /opt/blog/web/.output.bak-$(date +%F)
# 备份数据库（强烈建议）
mysqldump -u root -p blog > ~/blog-$(date +%F).sql
```

### 步骤 2 —— 数据库增量迁移（只需一次）

```bash
# 前提：应用至少正常启动过一次（WordCountMigrator 会自动补 article.word_count / user.huawei_id）
mysql -u root -p < blog-server/sql/perf-index.sql
```

- 报 `Duplicate key name` 属**预期**，忽略即可（幂等）。
- 只加索引 / 唯一约束，**不改列、不删数据**。
- ⚠️ 唯一约束 `uk_github_id` / `uk_gitee_id` / `uk_huawei_id` 若因历史重复数据失败，
  请先清理重复行再执行（重复会导致登录串号）。

### 步骤 3 —— 同步配置（关键）

**(a) `/opt/blog/blog.env` 新增：**

```bash
RAG_API_TOKEN=换成一段足够长的随机串
# 可选：如果生产前端域名不是 codeup.asia，显式指定跨域白名单
# CORS_ALLOWED_ORIGINS=https://codeup.asia,https://www.codeup.asia
```

**(b) `/opt/blog/application-prod.yml` 新增：**

```yaml
ai:
  rag-url: http://127.0.0.1:8000
  rag-token: ${RAG_API_TOKEN:}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:https://codeup.asia,https://www.codeup.asia}
```

> 注意：启动命令用的是 `--spring.config.location=/opt/blog/application-prod.yml`（**替换**默认位置），
> jar 内的 `application.yml` 在生产**不会被读取** → 新 key 必须写在这个文件里。
> 这三个 key 在代码里都有默认值，不配也能启动，但不配 RAG 就没有鉴权。

### 步骤 4 —— 构建并部署（一键脚本）

```bash
cd ~/Blog
chmod +x deploy/deploy.sh
./deploy/deploy.sh
```

脚本现在会依次做 6 件事：构建后端 → 构建前端 → 部署 `.output` 到 `/opt/blog/web/.output`
→ 同步两个 systemd unit + `daemon-reload` → 重启 `blog-api` → 重启 `blog-web`。

**手动等价操作**（不想用脚本时）：

```bash
# 后端
cd ~/Blog/blog-server
mvn clean package -DskipTests -pl blog-front-api -am
sudo cp blog-front-api/target/blog-front-api-1.0.0.jar /opt/blog/blog-api.jar

# 前端
cd ~/Blog/blog-web
npm install
npm run build
sudo mkdir -p /opt/blog/web
sudo rm -rf /opt/blog/web/.output
sudo cp -r .output /opt/blog/web/.output
sudo chown -R ecs-user:ecs-user /opt/blog/web

# 服务
sudo cp ~/Blog/deploy/blog-api.service /etc/systemd/system/
sudo cp ~/Blog/deploy/blog-web.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl restart blog-api blog-web
```

### 步骤 5 —— RAG 子系统

```bash
cd ~/Blog/rag_system/backend
echo 'RAG_API_TOKEN=上面那个一模一样的串' >> .env
# 重启你的 RAG 进程（systemd / supervisor / nohup 看你原先是哪种）
```

> - 服务现在绑定 `127.0.0.1:8000` ⇒ **必须与 Java 后端同机**，否则 Java 会连不上。
> - `reload=False` 已固定，生产不再跑开发模式。
> - 若之前为了调试把 8000 暴露到公网，记得关掉安全组/防火墙规则。

### 步骤 6 —— nginx

```bash
sudo cp ~/Blog/deploy/nginx.conf /etc/nginx/conf.d/blog.conf
sudo nginx -t
sudo systemctl reload nginx        # 或 restart
```

新增/确认了三处：`client_max_body_size 20m;`、`location /api/ai/`（关缓冲 + 300s 超时）、
`location /uploads/` 反代到 8080。

> `/sitemap.xml` **不需要**额外加 nginx 规则 —— 已由 Nuxt 的 `nitro.routeRules` 转发到后端。

---

## 四、逐批变更明细

### 第 1 批 · 安全与线上事故级

| 文件 | 关键调整 |
| --- | --- |
| `deploy/deploy.sh` | 绝对路径推导（任意目录可执行）、jar 名与 service 对齐、同步 unit + `daemon-reload`、预检、**新增前端 SSR 部署与 blog-web 重启** |
| `deploy/blog-api.service` | `ExecStart` → `/opt/blog/blog-api.jar` |
| `deploy/nginx.conf` | `client_max_body_size 20m`；`/api/ai/` 关闭缓冲 + `proxy_read_timeout 300s`；`/uploads/` 反代 |
| `CorsConfig.java` | `addAllowedOriginPattern("*")` → 显式白名单（读 `app.cors.allowed-origins`），凭证仍保留 |
| `RedisConfig.java` | 读侧 `SupportAutoType` 全局放开 → `autoTypeFilter("com.blog.")`，杜绝投毒反序列化 RCE |
| `UploadController.java` | 扩展名白名单（jpg/jpeg/png/gif/webp/bmp/ico/avif/pdf，**排除 svg**）+ Content-Type 复核 + 空文件拦截 + 日志 |
| `CommentController.java` | 长度 ≤500、校验文章存在且已发布、Redis 限流（同一用户同一文章 10 秒 1 条）、`comment_count` 原子自增 |
| `OAuthController.java` | `state` 下发时写 Redis（5 分钟 TTL），回调一次性校验并删除 → 修掉 OAuth CSRF |
| `AiController.java` | 调 RAG 时带 `X-RAG-Token` 头 |
| `application.yml` | 新增 `app.cors.allowed-origins`、`ai.rag-token` |
| `rag_system/backend/main.py` | 加 `verify_token` 依赖、绑 `127.0.0.1`、`reload=False` |
| `rag_system/backend/config.py` | 新增 `RAG_API_TOKEN` |
| `MarkdownRenderer.vue` | `html: true` → `false`，掐断 AI 回复 / PDF 注入导致的存储型 XSS |

> **注意**：评论**仍是自动通过**（`status=1`），本次只加了长度与频率限制，没有改成「先审后发」。

### 第 3 批 · 数据层与后端查询

- `blog.sql`：整合全部历史增量（`series` / `word_count` / 三个 openId / `source` / `operation_log`），
  并加入 `idx_list (status, is_deleted, is_top, create_time)`、`idx_tag`、三个 openId 唯一键。
- 新增 `perf-index.sql`：给**已存在的库**补上述索引（本次要执行的就是它）。
- 新增 `TagArticleCount` / `CategoryArticleCount` + 两条聚合 SQL：
  `/article/tags`、`/category/list` 从「全表载入内存 groupBy」改为一条 `GROUP BY`（消除 N+1）。
- 列表查询加列白名单（排除 `content_md` / `content_html` 两个 LONGTEXT）。
- `ArticleService`：`sort=title` 去掉错误的 `CAST(title AS UNSIGNED)`；浏览量改为
  Redis 计数 + 单条原子 `UPDATE`（不再读回整行）；归档 / 系列加 10 分钟缓存，写操作自动失效。
- 写接口补 `@Valid` / `@NotBlank` / `@Size`；删除分类时把子分类的文章 `category_id` 置空（修孤儿）。
- `WordCountMigrator`：迁移失败由 `log.warn` 改为 **抛异常 fail-fast**（免「启动成功但一查就 500」）。

### 第 2 批 · UI 与交互

- 后台 6 个列表页：`<table>` 外套 `overflow-x-auto`，手机端不再被裁掉且可横向滚动。
- 新增 `composables/useFeedback.ts` + `components/AppToast.vue` + `components/AppConfirm.vue`，
  替换全部原生 `alert()` / `confirm()`（18 处）。
- 主题一致性：去掉页眉/页脚/设置弹窗硬编码粉色；背景光斑、目录高亮、登录页 Canvas 粒子
  改用 `--ph` 等主题变量；`neon-glow` 补上浅色模式。
- 修掉 Tailwind 不存在的 `dark:bg-gray-750`（深色样式此前被静默忽略）。
- 编辑器两栏改 `grid-cols-1 md:grid-cols-2`；后台导航横向滚动；保存按钮加 `disabled` 防重复提交。
- 文章详情页：正文之外的「上下篇 / 作者卡 / 评论区 / 相关推荐」移进 `v-if`，404 时不再渲染空评论区。
- 无障碍：全局 `:focus-visible` 焦点环、分页/友链头像/复制按钮/弹窗关闭补 `aria`。
- 首页：右侧栏在 `xl` 以下隐藏时，主内容流底部补可折叠「热门内容」。

### 第 4 批 · 性能与 SEO

- **首页 SSR**：原先顶层 `init()` 未 await，SSR 首屏只有骨架屏且客户端重复请求；
  改为 `useAsyncData('home-init')` 服务端取好首屏 + payload 初始化，首屏 HTML 直接带文章。
- `/post/{slug}` 此前**整页零 SEO meta** → 补 `useSeoMeta` + Article JSON-LD + `watch` 重新拉取。
- 全站 `canonical`、`og:image`；列表/热榜/归档/上下篇/相关推荐内部链接统一优先 `/post/{slug}`。
- 新增 `GET /api/article/{id}/neighbors`：替代「拉 100 篇列表到客户端算下标」。
- 评论 / 相关推荐 / 上下篇改 `useAsyncData`（服务端拉一次、客户端复用）。
- 新增 `public/robots.txt`；`/sitemap.xml` 改由 Nuxt 转发，输出规范 URL + 首页。
- 清理未使用依赖：`mermaid`（npm 包）/ `@nuxt/icon` / `sharp`；`manualChunks` 只留 `highlight.js/lib/common`。

### 与部署无关

- `.github/workflows/ci.yml`：`concurrency` 取消旧运行、Actions 升 v5/v6、补 typecheck / 依赖审计 / RAG 语法检查。

---

## 五、上线后自测清单

| # | 检查项 | 期望 |
| --- | --- | --- |
| 1 | 首页查看网页源码 | **能看到文章列表**（SSR 首屏修复的核心） |
| 2 | 首页/文章页 `<head>` | 有 `canonical`、`og:image`、`application/ld+json` |
| 3 | `https://codeup.asia/sitemap.xml` | 返回 XML，URL 为 `/post/{slug}` |
| 4 | `https://codeup.asia/robots.txt` | 返回内容，Sitemap 指向 codeup.asia |
| 5 | 上传一张 > 1MB 图片 | 成功（不再 413）；上传 `.svg` 被拒 |
| 6 | 文章页图片 | 正常显示（`/uploads/` 反代生效） |
| 7 | 后台列表页（手机浏览器） | 可横向滚动，操作列可点 |
| 8 | 评论 | 能发；10 秒内连发第二条被提示「太频繁」 |
| 9 | AI 问答（长回答） | 不再 504，流式输出正常 |
| 10 | OAuth 登录 | 能成功（注意：现在依赖 Redis 存 `state`） |
| 11 | 文章页「上一篇 / 下一篇」 | 正常（新接口 `/article/{id}/neighbors`） |
| 12 | 改主题色 | 背景光斑 / 目录高亮 / 登录页粒子同步变色 |

---

## 六、回滚方式

```bash
# 1) 代码回滚
cd ~/Blog
git log --oneline -10          # 找到要退回的提交
git reset --hard 5020218       # 退回本次优化前的基线

# 2) 产物回滚（用步骤 1 的备份）
sudo cp /opt/blog/blog-api.jar.bak-YYYY-MM-DD /opt/blog/blog-api.jar
sudo rm -rf /opt/blog/web/.output
sudo cp -r /opt/blog/web/.output.bak-YYYY-MM-DD /opt/blog/web/.output
sudo systemctl restart blog-api blog-web

# 3) 数据库
# 本次只加了索引，回滚代码不需要删索引（留着反而更好）。
# 若确需还原数据：mysql -u root -p blog < ~/blog-YYYY-MM-DD.sql
```

---

## 七、明确**没有**做 / 已知遗留

1. **评论审核队列**：仍自动通过（`status=1`），未改为「先审后发」。
2. **RAG 的 pytest**：CI 暂以 `python -m compileall` 语法检查替代（依赖较重），待补用例后升级。
3. **完整前端构建未在本机验证**：本机 `nuxi build` 被沙箱拦截（清 `.nuxt/dist` 属批量删除）。
   已用 `@vue/compiler-sfc` 全量编译 35 个 `.vue` 验证语法（0 错误），
   **建议推上去让 GitHub Actions 代跑一次完整构建**。
4. **mermaid 仍是 `public/` 下的未压缩大文件**：`nitro.compressPublicAssets` 已生成 `.gz`/`.br`，
   若要生效需 nginx 对 `/vendor/` 开启 `gzip_static on;`。
5. **`f4b46ef` 之前的 `deploy/` 改动只在本机**：务必按步骤 4 / 6 手工同步到服务器。
6. **本机无 `origin/main` 缓存**：`git branch -vv` 可能显示 `[origin/main: gone]`，
   这是沙箱回滚本地 ref 导致的假象，用 `git ls-remote origin main` 判断才准。
